# Review of origin/claude/c3-t11a-reports-reverify-4611ey (PASS)

The branch adds library migration 0005 (nullable version_number and language on problem_report), library/reports.py with create_report(), apply_reverification() in proposals/apply.py, and two test modules; it stays inside T11a's owned paths and touches nothing of T11b's. It meets the brief's done-condition: run from a git-archive copy against the real Postgres, makemigrations --check is clean, the 34 tests in tests_reports, tests_apply and tests_library_fence pass, every new line of apply.py and reports.py is covered (the only miss, apply.py:143, is pre-existing vocabulary code), ruff, mypy and compliance_check --all are clean. The stamp is correctly the single exception: it lives in the allowlisted proposals/apply.py, opens library_write() with a reason, writes an append-only Verification row, changes only last_verified_at and verified_by on no_change (proved by a values() diff), requires a non-optional step_up_assertion_id and writes one tenant-less audit row through record(); main's fence test already names apply.apply_reverification as reverifyObligation's only writer. Report text reaches only problem_report.text: no logger in the module, the audit summary and after and the outbox payload are proved free of it, the audit row is tenant-scoped, and the cw_app RLS proof shows a report for another tenant is refused. Nothing on the branch builds a platform-side read of reports; what remains is wording written before Alex's 2026-09-19 answer, a tenant-less report path that answer makes pointless, and a test setUp that duplicates T4's builders now on main.

## Findings

### [LOW] brief: Docstrings still say a bleqq library editor reads the report
- Where: `backend/apps/library/reports.py`:4
- Scenario: Alex decided on 2026-09-19 (OWNER_RECOMMENDATIONS item 3, commit 6cf72f0) that a report stays inside the bank and no bleqq editor, agent or model reads it. The branch's wording predates that: reports.py:4-5 'a library editor answers it through a proposal (chunk 4)', reports.py:8 'the public title an editor will read', reports.py:16-17 'which is what an editor needs to open the same words'; models.py:484 (pre-existing) and 486-487 'so an editor reads the same words the reader read' and 'the console resolves it through a proposal (chunk 4)'; migrations/0005_problem_report_context.py:3-4 'so an editor opens the same words'. Nothing in the code reads a report, so this is wording only, but the next builder (T11b, chunk 4's GET /problem-reports) will read these docstrings as the design.
- Fix: Reword to the decided design: the report and its version/language context are for the bank's own reviewers; library deviations are found by the watch agents' re-check and corrected through a proposal. Same change in library/app.md INV-S7 ('created for the library editors') and proposals/app.md:121, which T11b owns.

### [LOW] brief: A tenant-less (platform-zone) problem report is a path nobody now needs
- Where: `backend/apps/library/reports.py`:38
- Scenario: create_report takes tenant_id: uuid.UUID | None and tests_reports.py:82-96 proves a platform user files a report with tenant None, giving a row in the platform zone plus a tenant-less audit row visible to every bank under the mixed policy. Under Alex's answer a report belongs to one bank and no platform person reads reports, so a tenant-less report has no reader and no bank. It also matters because H15 is still open on main (rls_operations(mixed=True) in migration_helpers.py:44-62 still lets a cw_app session with a bank active insert tenant_id NULL), so until H-C lands the only thing keeping a bank session's report inside the bank is the caller passing its own tenant id.
- Fix: Make tenant_id a required uuid.UUID, drop test_a_platform_reader_files_a_report_with_no_tenant, and have T11b's routes refuse a session without a tenant (the recommendation's 'the create routes refuse a session without a tenant' still applies). H-C then closes the database side.

### [LOW] conflict: ReverificationStamp.setUp rebuilds an instrument and obligation that T4's builders on main now provide
- Where: `backend/apps/proposals/tests_apply.py`:299
- Scenario: The brief says 'Use T4's builders in library/testing.py without editing them'; library/testing.py did not exist at the branch's merge base, so setUp opens library_write('test fixture') and creates Instrument and Obligation by hand (lines 299-320). Main now has testing.instrument(key=...) and testing.obligation(on, key=..., last_verified_at=...) which do the same. No textual merge conflict (main did not touch tests_apply.py), but the fixture is duplicated and diverges from every other library test.
- Fix: At merge, replace the two hand-built rows with `instrument = testing.instrument(key="fffs-2017-2")` and `self.obligation = testing.obligation(instrument, key="fffs-2017-2/9/6", last_verified_at=WAS_VERIFIED_AT)`; the assertions need no change.

### [LOW] simplicity: reporter and verified_by are typed Any instead of identity.User
- Where: `backend/apps/library/reports.py`:39
- Scenario: create_report(reporter: Any) and apply_reverification(verified_by: Any) accept anything; a caller passing a Membership instead of its .user would fail only at the database FK. mypy passes because Any hides it.
- Fix: Import apps.identity.models.User in both modules (library/models.py already references "identity.User" so there is no cycle) and type the parameters as User.

## Conflicts with main

No textual conflicts: main has not changed any of the six owned paths since the merge base, and main's library migrations stop at 0004, so 0005_problem_report_context collides with nothing. Three things on main change the context: (1) T4 landed backend/apps/library/testing.py, whose instrument()/obligation() builders the brief told this task to use; tests_apply.py rebuilds the fixture by hand (finding above, swap at merge). (2) Commit 6cf72f0 recorded Alex's answer that reports stay in the bank; the branch's docstrings in reports.py, models.py and 0005 still describe an editor reading the report (finding above). (3) H-C (mixed tables write only their own zone) has not landed, so the problem_report policy is still one FOR ALL with the read rule as the write check; the branch neither fixes nor worsens that and its RLS test pins only the cross-tenant refusal. main's fence test already names apps.proposals.apply.apply_reverification as reverifyObligation's writer, so the branch supplies what main expects rather than duplicating it.

## Checked clean

Owned paths respected (six files, all in the brief). Migration 0005 depends on 0004, adds two nullable columns only, no JSONField, no new enum; makemigrations --check clean against the local database. Stamp: only in proposals/apply.py (allowlisted, allowlist unchanged), inside library_write() with a reason, Verification row append-only via 0004's trigger, only last_verified_at and verified_by change on no_change (values() diff test), other outcomes write a row and leave the stamp, step_up_assertion_id is a required parameter and lands on the audit row, tenant_id None, note kept out of audit after; outcome validated against VerificationOutcome; the runtime fence refuses the same save outside library_write(). Reports: tenant and reporter are parameters the caller sets from the principal, no body input; text only in problem_report.text, proved absent from audit summary/subject_title/before/after and outbox payload; no logging in the module; audit row tenant-scoped so it is not platform-visible; audit and outbox share the caller's transaction (rollback test); cw_app proof that a report for another tenant is refused by RLS; language validated against active Language rows; version_number >= 1; description trimmed and required. No route, schema, reader or console code is added, so nothing builds toward a platform-side read. Gates run from an extracted copy on a review-only test database: 34 tests green, new code 100% covered, ruff, mypy and compliance_check --all clean. Tests fail without the change (both modules import the new symbols; the cw_app insert uses the new columns).