# Review of origin/claude/c4-t7-console-tenants-si8mjk (PASS)

The branch (one commit, 2ea9c49) adds GET and POST /console/tenants behind tenants.manage: creation writes the Tenant, activates only that tenant, runs set_content_languages, ensure_system_roles and ensure_tenant_vocabularies, records tenant.created under the new tenant id, and calls invitation_logic.create_invitation with the role that holds members.manage, all inside the request transaction; a taken slug answers 409 duplicate_key. It meets the brief's done-condition: ADM-S6 narrowed and un-skipped, ADM-S4 reworded and un-skipped as a data-driven test that enumerates every platform-gated operation via iter_operations and drives it with both platform roles, TEN-S6 gains the support-access clause and stays skipped, FIRST_RUN_SETUP steps 6 and 7 rewritten, and the diff stays inside the eight owned paths. The security bar holds: permission constants only (api.py:86,93), no role-name lookup (logic.py:299 derives the admin role from MEMBERS_MANAGE), no step-up (matching inviteMember on main and the brief), both audit rows carry the new tenant id (tests_console_tenants.py:test_every_audit_row_the_call_writes_belongs_to_the_new_tenant), the invitation reuses the existing hashed 72-hour token and emailed-code path with the token never returned or logged, no logger calls added, and a platform-role holder is refused as first admin (logic.py:268) so the c4-T4 failure mode cannot recur from this side. Input validation covers name, slug, timezone, languages and email. The only defect found is a low-severity check-then-insert race on the slug. Merge is clean against local main (git merge-tree reports no conflicts; main's changes to the same three files are in different hunks), but openapi.json and api.generated.ts were deliberately left unregenerated, so the merger must run generate-types.sh before shipping or CI's openapi-types-drift job goes red.

## Findings

### [LOW] correctness: Slug uniqueness is check-then-insert, so a concurrent duplicate answers 500 instead of 409
- Where: `backend/apps/tenants/logic.py`:259
- Scenario: Two platform admins submit POST /console/tenants with slug 'example-bank' at the same moment. Both pass `Tenant.objects.filter(slug=cleaned_slug).exists()` at line 259, the second `Tenant.objects.create` at line 273 hits the unique index on tenant.slug and raises IntegrityError, which the problem handler turns into a 500 rather than the 409 duplicate_key the brief specifies. Integrity is preserved by the constraint; only the answer is wrong.
- Fix: Drop the pre-check and wrap `Tenant.objects.create` in `try/except IntegrityError` raising `ValidationError(..., code="duplicate_key")` (inside a savepoint via transaction.atomic() so the request transaction stays usable), or keep the pre-check and add the except as the backstop. test_a_taken_slug_answers_409 still passes either way.

### [LOW] conflict: Generated contract files are not regenerated; CI's drift gate fails on the merged tree
- Where: `openapi.json`:1
- Scenario: The branch adds two routes and three schemas but, as its commit body states, leaves openapi.json and frontend/src/types/api.generated.ts untouched (the cloud-session rule). After merge into main, the openapi-types-drift job (.github/workflows/ci.yml:261-291) runs generate-types.sh and `git diff --exit-code` on those two files, which now differ, so the push is red.
- Fix: On the merge commit run `bash generate-types.sh` and commit openapi.json and frontend/src/types/api.generated.ts alongside the merge, as CLAUDE.md section 7 item 6 requires.

## Conflicts with main

No textual conflicts: `git merge-tree --write-tree main origin/claude/c4-t7-console-tenants-si8mjk` produces a clean tree. Main changed three of the branch's eight files since the merge base, all in non-overlapping hunks: governance/app.md (AUD-01 status to built, AUD-S2 Gherkin gains the maintenance-hatch clauses), governance/tests_scenarios.py (test_aud_s2 now also proves append_only_guards.EveryAppendOnlyTableRunsAGuard, plus its import), and FIRST_RUN_SETUP.md line 22 (agents path). tenants/logic.py, api.py, schemas.py and app.md are unchanged on main. No duplicate work: main still has only consoleReissueEnrolment under /console/tenants, and the set of platform-permission-gated operations on main (listProposals, getProposal, approveProposal, rejectProposal, consoleReissueEnrolment) plus the branch's two equals PLATFORM_ROUTE_REQUESTS exactly, so ADM-S4's first assertion (tests_scenarios.py:238) holds on the merged tree; UNGATED_BY_DESIGN on main still lists ("POST", "/proposals") as a logic gate. After the merge, the frontend governance.journey.spec.ts:96 fixme title still reads the old ADM-S6 wording; requirements_coverage only matches on the ID so it stays green, but the title should be updated when the console screen lands. openapi.json and api.generated.ts must be regenerated at merge (see finding).

## Checked clean

Brief scope: diff touches exactly the eight owned paths; GET lists name, slug, status, default language, created; POST takes the specified body; no member counts, no tenant detail route; FIRST_RUN_SETUP steps 6 and 7 rewritten. Invariants: both routes gated by perms.TENANTS_MANAGE (api.py:86,93), never a role name; first-admin role derived from MEMBERS_MANAGE (logic.py:299) and roles_by_keys refuses an empty list; no step-up, consistent with inviteMember on main and playbook 4.2; tenant.created and invitation.created both written through record() with tenant_id=tenant.id inside ATOMIC_REQUESTS, and tenancy.activate runs after the Tenant insert and before every tenant-table write (logic.py:273-308), so under cw_app RLS every row lands under the new tenant and no other tenant's rows are readable; the platform session activates nothing else. Invitation path: reuses create_invitation (hashed token, INVITATION_TTL_HOURS, fragment link, emailed code), the token is returned nowhere (create_tenant returns the Tenant; ConsoleTenantRow has no token field) and no logger call is added; invited_by=None. No platform role reaches the bank admin: get_or_create_user creates a plain user with TenantRole links only, and logic.py:268 refuses any address holding a PlatformRoleAssignment, proven by test_a_platform_account_cannot_be_a_banks_first_admin. Validation: name stripped and non-empty (max 200), slug lower-cased and slugify-equal (max 80), IANA timezone, active Language rows for default and content languages, normalise_email; every refusal has a test and happens before any write. Tests: all new tests exercise the new routes so fail without the change; test_adm_s4 and test_adm_s6 named per convention with the ID in the docstring; ADM-S4 subtests prove 403 with requiredPermission for the non-owning role on all seven gated operations and on POST /proposals for platform_admin, and that the owning role is never refused with permission_denied. Compliance lint: no kwargs, typed responses, no enums added, no JSONField, no logger. Simplicity: no speculative abstraction; the platform-account refusal is a security guard with a stated rationale, not scope creep. Mail: eager in tests, on_commit in production.