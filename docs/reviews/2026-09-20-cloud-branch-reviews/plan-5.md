# Review of origin/claude/plan-chunk-5-s69w52 (BLOCK)

The branch adds docs/plans/briefs/CHUNK5_TASKS.md: 44 chunk 5 tasks in 12 waves with a scenario-ownership table, rulings A to G, defaults, cuts, and three questions for Alex. It matches Build_Plan chunk 5 and PARALLEL_PLAN 3.3 in scope and style, and every chunk 5 requirement has an owning task. It was written before Alex's answers of 2026-09-19 and before E3/E5 merged, so it now needs a re-check task, a platform-owned-agents statement, and the removal of the problem-report console surface. Two guard gaps must be fixed first: watch/logic.py stays on the library_write allowlist while two tasks write it, and ai_generation is confirmed from a tenant session on a library row. Questions the plan says need Alex: q-watch-fence is already answered by the repository (fence test, PLAYBOOK 14 and CONVENTIONS name watch/logic.py as the watch pipeline's library_write door; ID-S21 already names the inventory tables), so the remaining choice is engineering, not an invariant; q-so-what-scope is answered by D-07 and D-32 (Option A); q-case-urgency-at-creation is answered by Alex's item 1 plus the watch and change cards, which show a suggested urgency before triage (Option A). Inherited: q-urgency and q-search-fence are answered (items 1 and 4, so the c5-search-similar fallback is moot); D-45's lawyer answer and k-anthropic remain open.

## Findings

### [HIGH] invariant: watch/logic.py keeps its library_write() door while f03-T41 and f03-T43 write that module
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:313
- Scenario: backend/apps/shared/tests_library_fence.py (and PLAYBOOK 14, CONVENTIONS) allowlist `watch/logic.py` for `library_write()`, which reaches every LibraryModel including instrument, provision and obligation. c5-contract-models-watch adds `watch_write()` and four new modules but never removes `watch/logic.py`, and plan rule 8 forbids any other task touching the fence. f03-T43 (line 1882) and f03-T41 (line 2064) then own and write `backend/apps/watch/logic.py`, so a watch module can write an inventory row through the old door and the plan's own done-condition ('no inventory table is reachable from a watch module') is false.
- Fix: c5-contract-models-watch removes `watch/logic.py` from LIBRARY_WRITE_ALLOWLIST and the PLAYBOOK/CONVENTIONS rows in the same fence edit, and its fence proof asserts that no module under apps/watch/ may call library_write(). Alternatively keep one door and make the runtime check refuse inventory models from watch callers; either way the security review's 'exactly the three named modules' (line 2195) must match the four the task lists.

### [HIGH] invariant: A tenant session writes the library ai_generation row, and the mixed-table policy shape is unspecified
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:1694
- Scenario: c5-ai-log-and-so-what-draft writes one AiGeneration row per change with no tenant (a library row). c5-cases-so-what-and-links then moves that row's review state to confirmed from a tenant session on POST /so-what/confirm. Two tenants confirming would overwrite one shared row, and under the H-C split policy (HARDENING H15, high: a bank session must not write platform rows) the write is refused; if built with `rls_operations(mixed=True)` before H-C lands, tenant A can rewrite platform rows at the database level. c5-ai-log-contract (line 716) says only 'enabled and forced'.
- Fix: c5-ai-log-contract builds ai_generation with the split policy agent_run already has (FOR SELECT mixed read, FOR ALL write limited to the session's own zone) and lists it in tests_rls, or depends on H-C. The tenant's confirmation stays on change_case (so_what_confirmed_*); the library row's review state is set only by a platform action or a per-tenant review row, and AUD-S4's 'its row's review state is confirmed' is reworded for chunk 7 accordingly.

### [MEDIUM] brief: No task builds the library re-check Alex decided on (OWNER_RECOMMENDATIONS item 3)
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:34
- Scenario: Alex decided that a bank's problem reports stay inside the bank and that the watch agents re-check library records against their sources on every run and propose corrections through the proposal door. The plan's 'What it delivers' and every task predate that answer: nothing lets a key read a record's citations, nothing logs a re-check against a source_document, no prompt or definition rule tells the sweeper to re-check, no eval row covers a drifted record, and AUD-03's 'closing the loop to the agents' has no chunk 5 owner.
- Fix: Add a task (for example c5-library-recheck, after c5-library-links-provenance and c5-watch-sources-coverage): an agent key with library:read reads live records with their citation and source_document; a re-check is logged as a source check of a new check kind; a changed content hash or a contradicted field yields an update_obligation or retire_record proposal through apply.py, never a direct edit; f03-T44 and f03-T45 add the re-check rules to the prompt and definition; f03-T46 adds a drifted-record eval row; a scenario is added to agents/app.md.

### [MEDIUM] brief: ADM-S4 reword lists problem reports as a library-editor console surface that no longer exists
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:1599
- Scenario: c5-fe-console-sources rewords ADM-S4 so a library editor reaches 'problem reports' and un-skips the journey against it. Under item 3 no bleqq editor reads a report, chunk 4's Q1 tail (T16a, T16b, T18, T21, T22, T23, T24b) is replanned, and PRO-03 and AUD-03 change in PRD 0.4; the journey would either assert a surface that must not exist or fail.
- Fix: Reword ADM-S4 without problem reports (queue, vocabularies, sources, Change facts for the editor; tenants and Agent keys for the platform admin) and name the report loop as replaced by the re-check; c5-fe-change-detail's 'This looks wrong' dialog hint must not say a bleqq editor reads the report.

### [MEDIUM] brief: Platform-owned agents (item 14) are not stated, and c5-agent-runs lets a tenant key open a run that c5-platform-agent-keys forbids
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:815
- Scenario: Alex decided bleqq's watch agents are part of the base package, platform-owned and platform-run, and that tenant controls apply only to tenant-added agents that write only to their own zone. c5-agent-runs says 'a tenant key opens one under its tenant', while c5-platform-agent-keys strips agent-runs:write from tenant keys in R1; no task states that Agent rows loaded from definitions are platform-owned and unreachable by any tenant route, and chunk 11's plan will read this brief.
- Fix: In c5-agent-runs: R1 runs are platform runs only; a tenant-bound key or session asking to open a run answers 403 with the reason named, proved by a test. Add a plan-wide default citing item 14: bleqq's agents and their definitions are platform-owned, no tenant route lists them as editable, AGT-04's controls (chunk 11) reach only tenant-added agents, and tenants read library runs on GET /agent-runs read-only.

### [MEDIUM] brief: Wave 1 is blocked on q-watch-fence, which the repository already answers
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:258
- Scenario: The plan holds c5-contract-models-watch and every watch writer until Alex answers whether the watch zone is inside the library fence. tests_library_fence.py's docstring and allowlist, PLAYBOOK 14 and CONVENTIONS already name 'the watch pipeline' (watch/logic.py) as an allowlisted library_write() caller, and ID-S21's Gherkin already names instrument, provision and obligation routes, so the reword the plan wants approved is already the text. Likewise q-so-what-scope is D-07 and D-32, and q-case-urgency is item 1 plus the tenant-watch and tenant-change cards, which render a suggested urgency before triage.
- Fix: Convert the three questions into 'Defaults taken' (Option A each, with the sources named) so nothing waits; keep only the narrower question worth asking Alex: whether a library editor's one-person confirmation of a suggested classification or link (no proposal, no step-up, no second person) is an accepted second exception to 'proposals are the only door', or must go through a confirm_change_facts proposal kind applied by apply.py.

### [MEDIUM] correctness: Console Sources 'Check now' is chunk 11 by the plan's own cut list and calls a key-only route
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:1593
- Scenario: The cut list says no research_request or runner is built (AGT-05 is chunk 11), yet c5-fe-console-sources builds 'Check now', which is AGT-05's 'check a source now'. It calls POST /agent-runs, declared under the agent-runs:write scope (key only) in c5-contract-api-agent, from a session holding sources.manage, and reads GET /agent-runs, gated agents.manage or system.health, neither of which a library editor holds; the screen would get 401/403 and the journey fails.
- Fix: Drop 'Check now' from c5-fe-console-sources (c11-platform-runs owns it) and keep the page read-only, or declare a session route under sources.manage in c5-contract-api-screens and give GET /agent-runs a sources.manage read for library runs.

### [MEDIUM] correctness: Two console screens call routes no contract task declares
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:1308
- Scenario: c5-fe-console-change-facts lists 'changes waiting for confirmation', but the only list route is GET /changes (watch.read), which c5-watch-feed-read joins to the reader's own tenant case; a platform console session has no tenant and no case, and no unconfirmed filter is declared. c5-fe-console-agent-keys lists, creates and revokes platform keys, but main has only /tenant/api-keys under a tenant session, c5-contract-api-agent adds only agentId to identity/schemas.py, and c5-platform-agent-keys owns api_keys_logic.py, not identity/api.py. Both violate plan rule 2 (no screen calls a stub or an undeclared route).
- Fix: c5-contract-api-screens declares a console list of unconfirmed changes under proposals.review (served by c5-watch-feed-read without the case join) and c5-contract-api-agent declares the platform key routes (list, create with step-up, revoke) under agent_definitions.manage, holding the idapi key; both screens name those routes and their serving tasks in depends-on.

### [MEDIUM] conflict: Owned-path collisions inside waves and a model split across two tasks
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:206
- Scenario: Wave 5: c5-watch-registration and c5-watch-sources-coverage both own backend/apps/watch/tests_scenarios.py; wave 7: c5-cases-so-what-and-links and f03-T43 both own it; tests_scenarios.py is not in rule 6's append-ledger list, so the wave rule ('disjoint owned paths') is broken. c5-cases-so-what-and-links (line 1702) adds a tenant link table to cases/models.py and a migration, which plan rule 1 reserves to c5-contract-models-cases, and it holds no mig:cases key. c5-cases-footprint-hooks says it edits cases/creation.py (line 1160), which c5-cases-creation owns and which is absent from its owned paths.
- Fix: Add each app's tests_scenarios.py to the rule 6 ledger with 'own skip line only'; move the case-side obligation-link table into c5-contract-models-cases; give c5-cases-footprint-hooks its own module only, or add cases/creation.py to its owned paths and serialize it after c5-cases-creation.

### [MEDIUM] conflict: c5-e2e-j4-agent depends on c5-agent-api-flow, so the chunk 7 contingency cannot work
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:2147
- Scenario: The contingency (line 121) says J-4 needs nothing from chunk 7 and drops the similar step if chunk 7 is late, but c5-e2e-j4-agent's depends-on names c5-agent-api-flow, which waits on c7-hybrid-search-backend and c7-index-changes; by the plan's own start rule the smoke journey and c5-chunk-close then wait on chunk 7 anyway.
- Fix: Remove c5-agent-api-flow from c5-e2e-j4-agent's depends-on (its real backend dependencies are already listed) and state that the similar step is added by c5-agent-api-flow when it lands; also note that q-search-fence is answered (item 4), so the c5-search-similar fallback text can go.

### [LOW] simplicity: Several tasks are far beyond thirty minutes of sub-agent work
- Where: `docs/plans/briefs/CHUNK5_TASKS.md`:1437
- Scenario: c5-library-links-provenance adds six proposal kinds, two library tables with migrations, citations on apply, verification links and a registration edit (chunk 4 spent one task on one kind); c5-contract-models-watch builds seven models, the fence rewrite, kinds and RLS entries; c5-watch-feed-read serves the twelve-filter feed with query pins plus three more routes; c5-seed-watch, c5-e2e-watch-journeys (six journeys including an sv run), c5-fe-console-change-facts (list, detail, four write flows) and c5-fe-watch-feed (feed, filters, coverage tab, budget measurement) are each a multi-hour package.
- Fix: Split: provenance into (a) source_document and citation models with the fence proof and (b) the six kinds in apply.py; the feed read into the feed and the three secondary routes; the watch journeys into two tasks of three; Change facts into list and detail; the watch feed into the feed screen and the coverage tab.

## Conflicts with main

No file collision: docs/plans/briefs/CHUNK5_TASKS.md does not exist on main. Stale premises: E3 and E5 are merged on main (shared/adapters/llm.py has AnthropicLlm and stream(); agents/models.py has Agent and AgentRun; identity ApiKey.agent; agents/seeds/; agents/screen.py), so the 'in review' preamble and the 'assumes all four are merged' note are outdated; f03-T37 (watched markets, taxonomy 0004) and H10 (no query text in logs) are on main, so those preconditions already hold. Alex's answers (OWNER_RECOMMENDATIONS items 1, 3, 4, 14; commits 6cf72f0 and 3cc373a) landed after the merge base: the plan's open-questions section and c5-chunk-close's instruction to copy the three questions into TODO_FOR_alex.md would duplicate rows that are already answered there; q-Q1's row must be read as decided. design/screens/tenant-watch.html and tenant-change.html changed on main (markets, participants), so the screen tasks must read the current cards. The definitions live at backend/agents/watch-sweeper/v1/ (IMPLEMENTATION_STATUS says the directory moved), while the plan's owned paths write `agents/watch-sweeper/v1/...`; resolve to the backend path. Resolve by rebasing the plan text on main, restating the four E-packages as done, and rewriting the open-questions section per the findings above.

## Checked clean

Scenario ownership table gives exactly one owner per half and matches each task's Scenarios line; rulings A to G are consistent with PARALLEL_PLAN 3.2 rulings 1, 3, 4, 5, 9, 16, 19, 31, 32; every Build_Plan chunk 5 requirement (WAT-01 to 05, WAT-07, AGT-01, 02, 07, 08, CAS-01, ID-10, FP-04, J-4, AUD-02 model half) has at least one building task; the watchwrite, watchfe, apply/prop, sweeper and eval chains run one task per wave; step-up on platform key creation and the once-shown hashed secret are kept; record() in the same transaction, @tenant_task on the cursor, settings with env overrides, generated files never committed, coverage floors ratchet only, no route left at 501 at close, CLAUDE.md section 11 propagated to UI tasks; the outbox cursor's published_at update is allowed by the existing cw_outbox_guard (shared/models.py); the cut list matches PRD releases (WAT-06 R3, CAS-02 to 08 and AGT-03 to 06 R2); f03-T41 to T47 match FEATURES_0_3_TASKS.md owned paths and done-conditions; chunk 6's brief depends on the same c5 task names.