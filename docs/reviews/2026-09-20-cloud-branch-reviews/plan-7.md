# Review of origin/claude/plan-chunk-7-iwelpw (BLOCK)

The branch adds one file, docs/plans/briefs/CHUNK7_TASKS.md: 20 tasks in 11 waves for search, Ask, the AI log and the evaluation set. It covers SRC-01/02/03/05, AUD-02, ADM-02's evaluation surface and J-7, gives every SRC scenario except S7/S13 one owner, keeps read-only POSTs off the audit guard without touching it, cuts search_query_log and POST /eval/runs with reasons, and its wave owned paths are disjoint apart from declared ledgers. It was written before Alex's 2026-09-19 answers and now contradicts them.

Questions the plan says need Alex: (1) q-search-fence: already answered, item 4 "OK" accepts Option B (derived model, own fence, allowlist unchanged, package split in two); the plan recommends Option A and adds a fence allowlist entry. (2) k-embedder: still open (TODO_FOR_alex line 10); item 7 now limits production candidates to Bedrock EU or none. (3) k-anthropic: D-07 already fixes the test environment to anthropic and item 7 exempts test with a banner; the cost/approval line for typed questions is not recorded. (4) Scope applied to search by default: nowhere answered (D-24 exempts only My work); stays a default to confirm. Item 3's library re-check is chunk 5's; no new chunk 7 task is needed, but c7-index-changes and c7-ask-switch need rewording for items 4, 10 and 14 (findings).

## Findings

### [HIGH] brief: c7-search-index plans the rejected Option A and a library-fence allowlist entry after Alex accepted Option B
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:345
- Scenario: OWNER_RECOMMENDATIONS item 4 (Alex: OK) makes search_chunk a derived model with its own index_write() fence and AST guard, RLS in the agents-0001 shape (FOR SELECT shared-or-mine, FOR ALL own zone), owner_tenant_id always NULL in R1, library reads in search/sources.py, embeddings filled by the worker from outbox events with no tenant active, and splits the package into (a) model/fence/RLS under mig:search and (b) the apply.py hook, sources.py and rebuild command under the apply key, with an ADR, a DECISIONS row and an INPUT_DELTAS line. The plan instead recommends Option A, owns tests_library_fence.py to add search/indexing.py to LIBRARY_WRITE_ALLOWLIST (rule 7, c7-search-index owned paths, c7-security-review 'the allowlist grew by exactly one entry'), copies the H15-broken mixed policy, and queues embedding through transaction.on_commit plus a Celery task, a second relay that PARALLEL_PLAN ruling 9 and the plan's own Cut list forbid. Built as written it adds a fourth writer to the library zone against the owner's decision.
- Fix: Rewrite c7-search-index to item 4: split into c7-search-index-model (SearchChunk outside LibraryModel, index_write() runtime fence, AST guard allowing writes only in search/indexing.py, split RLS policies listed in tests_rls OTHER_POLICIES, cw_app test that tenant A cannot insert, change or delete a shared chunk) and c7-search-index-apply (apply.py hook, sources.py, reindex_library command, one system audit row with counts on a rebuild). Remove the fence allowlist entry from owned paths, rule 7, the wave notes and c7-security-review; embed from outbox events through c5-outbox-cursor with no tenant active; add the ADR, DECISIONS row and INPUT_DELTAS line (schema.sql labels the table LIBRARY); mark q-search-fence answered.

### [HIGH] invariant: c7-eval-sets writes library rows from a console route and from a seeds directory the fence does not allow
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:869
- Scenario: EvalQuestion and EvalRun are built 'as library tables' (schema.sql: LIBRARY) with POST /eval/questions writing them under eval.manage and a seed in backend/apps/search/seeds/__init__.py. tests_library_fence.py allows library_write only from proposals/apply.py, watch/logic.py and the seed dirs shared/management/commands, library/seeds, taxonomy/seeds, agents/seeds, and ProposalDoorGuard demands that only approveProposal and reverifyObligation reach a library write. The task cannot go green without loosening both guards, which rule 7 and CLAUDE.md §5 (proposals are the only door into the library) forbid, and the plan's own invariant line asserts the opposite of the guard.
- Fix: Model eval_question and eval_run as platform tables outside LibraryModel (no tenant column, forced RLS that refuses any tenant session, written through record() under eval.manage and from a management command), record the departure from schema.sql's LIBRARY label in INPUT_DELTAS, and keep the fence allowlist and ProposalDoorGuard untouched. State this in the task's invariants and in c7-security-review.

### [HIGH] test: c7-eval-gate cannot record the retrieval baseline on the mock embedder without lying to search_eval.py
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:623
- Scenario: search_eval.py record() refuses any evaluator with is_mock=True and decide() only fails a track once it is recorded. Until k-embedder arrives the only embedder is the mock (EMBEDDER_PROVIDER=mock, RERANKER_PROVIDER=mock), so a Django Retriever honestly reporting is_mock=True cannot satisfy 'record the baseline in the same commit with --record', 'SRC-S8 exits non-zero when a score drops' or 'a deliberate regression fails the gate'. The only route to green is declaring is_mock=False while the vector leg runs on the mock, which records a fake baseline and lowers the gate; f03-T49's 'removing the guard makes search_eval.py fail' and its re-record inherit the same problem.
- Fix: Define the retriever's is_mock as 'any adapter in the chain is a mock'. c7-eval-gate wires the retriever, prints per-language scores, proves the not-yet-recorded path and the self-test's synthetic recorded baseline (which already exits non-zero), and adds the no-expected-keys rule; the real baseline is recorded once by c7-embedder-selection-baseline after k-embedder. Reword SRC-S8's done-condition, f03-T49's re-record clause and c7-chunk-close accordingly, and name the unrecorded track in TODO_FOR_alex.

### [MEDIUM] conflict: c7-embedder-mock-reranker is already merged on main; the plan still schedules its review and merge
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:232
- Scenario: Commit d10daf8 on main ('A Danish question finds the Swedish obligation...') is package E4: the concept-aware MockEmbedder, reranker.py, RERANKER_PROVIDER/RERANKER_TOP_K, MOCK_ADAPTER_SETTINGS and the production-guard test. The plan's wave 1, its critique note ('already built and waiting for review') and c7-e2e-seed's dependency treat it as an unmerged cloud branch to review from RemoteTrigger.
- Fix: Mark c7-embedder-mock-reranker done at d10daf8, drop it from wave 1 and from the 'in review' text, and keep it only as a satisfied dependency of c7-hybrid-search-backend, c7-e2e-seed and c7-embedder-selection-baseline.

### [MEDIUM] brief: c7-index-changes indexes tenant-owned changes with an owner, against items 4 and 10
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:684
- Scenario: The task says 'a tenant-owned change carries its owner' and the handler activates the tenant through @tenant_task. Item 4 (accepted) says owner_tenant_id is always NULL in R1, only shared rows are indexed until an ADR supersedes ADR 0010, and embeddings are dispatched with no tenant active; item 10 (accepted) says no owned row or child of one is indexed, embedded, reranked or sent to a model and find-similar returns shared rows only.
- Fix: Index platform (shared) changes only; a change event carrying a tenant owner is skipped with a test proving no chunk and no embedding request; the handler runs with no tenant active. Reword the H7 language in c7-search-index, c7-hybrid-search-backend and c7-security-review to 'shared rows only in R1'.

### [MEDIUM] brief: c7-ask-switch's 'one AI switch for every AI feature' must stop at bleqq's platform agents (item 14)
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:299
- Scenario: The toggle copy says turning it off 'stops Ask and the drafts a model writes' and the task calls Tenant.ai_enabled the switch for every AI feature. Item 14 (Alex's own) makes bleqq's general watch agents platform-owned: no tenant can switch them off, pause them or change them; tenant controls apply only to tenant-added agents. A wrapper check on ai_enabled that also covers library runs would give a tenant that control.
- Fix: State in the task that the switch governs tenant-zone AI only (Ask, the tenant's WAT-05 drafts, and from chunk 11 the tenant's own agents), that the logging wrapper reads it only when a tenant is active, and add a test that a platform (library) model call ignores it. Reword the toggle copy to the tenant's own features.

### [MEDIUM] brief: The search contract's R1 SearchFilters cannot carry the jurisdiction and duty-type filters SRC-S3 and the screen need
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:267
- Scenario: c7-search-api-contract limits SearchFilters to instrumentId, termIds, binding and inFootprint. SRC-S3 filters by jurisdiction 'SE' and duty type 'reporting', and c7-search-screen draws a jurisdiction and a duty-type filter. Jurisdiction is its own table (library/models.py Jurisdiction) and duty type is a Vocabulary (taxonomy DutyType), not a taxonomy term, so neither fits termIds; c7-hybrid-search-backend would find the contract wrong and stop (rule 2).
- Fix: Add jurisdictionId and dutyTypeId (keys) to SearchFilters in the contract task with an INPUT_DELTAS row, carry both in search_chunk.metadata in c7-search-index, and filter on them before ranking in c7-hybrid-search-backend.

### [MEDIUM] conflict: AUD-S4's integration half needs chunk 5's So-what draft and confirm, which run in the same global wave as c7-ai-log-backend
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:465
- Scenario: AUD-S4 starts from 'a So what draft, an Ask answer and an agent classification' and confirms the So what (POST /changes/{id}/so-what/confirm, chunk 5). c7-ai-log-backend (global wave 6, chunk-local wave 3) depends only on c5-ai-log-contract and c7-search-api-contract, so it runs beside c5-watch-curation (global wave 6) and before Ask exists (wave 4); its 'confirmation sets the reviewer and time' also duplicates the chunk 5 confirm route. There is no CHUNK5_TASKS brief to check which chunk 5 package owns governance/ai_log.py, which c7-search-api-contract (wave 1, depends on nothing) also creates.
- Fix: Make c7-ai-log-backend own only filters, feedback and the mark-reviewed function chunk 5 calls; move the AUD-S4 un-skip to c7-ask-backend (or a later task) with c5-watch-curation and the classification producer as dependencies; put the feedback stub in a file c5-ai-log-contract does not create, or make c7-search-api-contract depend on c5-ai-log-contract.

### [MEDIUM] invariant: The AI switch write is gated by security.manage but asks no step-up
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:308
- Scenario: Playbook 4.2 lists step-up on security policy changes, and CLAUDE.md §5 on security changes. Switching Ask on decides whether tenant-typed text leaves the tenant to a model (D-07); the task audits the write but adds no @requires_step_up and states no default for leaving it out.
- Fix: Add @requires_step_up to the aiEnabled write (or a separate PATCH) with the step-up id on the audit row, or write the default and its reason in the task, the commit body and TODO_FOR_alex.

### [MEDIUM] brief: c7-embedder-selection-baseline ignores item 7: production accepts only Bedrock EU or none for the embedder and reranker
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:1361
- Scenario: Item 7 (accepted) refuses non-EU processors at production boot: the embedder and reranker must be none or bedrock in an EU region, and 'until D-09's embedder has an EU endpoint, production search is keyword-only'. The task scores any candidates and requires that 'production boots with the real provider', which a US-hosted candidate cannot satisfy.
- Fix: Limit candidates to EU-hostable models (Bedrock EU or none), or state that the chosen provider is test-only until an EU endpoint is logged, and change the done-condition to 'the test environment boots with the real provider; production stays keyword-only until c14-eu-data-location admits it'.

### [LOW] brief: c7-eval-gate re-adds a CI and prepush gate that already exists
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:635
- Scenario: .github/workflows/ci.yml line 256 and scripts/prepush.sh line 185 already run search_eval.py (prepush under --all). The task owns ci.yml and prepush.sh to 'add the gate', taking the local-only ci key for no change.
- Fix: Drop the ci.yml/prepush.sh edits and the ci key; if the prepush tier should trigger on search-file changes, name that one-line change only.

### [LOW] brief: f03-T49 re-records baseline.json but does not own it
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:1163
- Scenario: The task text re-records the retrieval baseline if the added row moves a metric, yet its owned paths are retrieval.jsonl and tests_scenarios.py only; baseline.json belongs to c7-eval-gate and c7-embedder-selection-baseline.
- Fix: Either add backend/eval/baseline.json to f03-T49's owned paths (serialized by the eval key) or, per the eval-gate finding, drop the re-record clause until a real baseline exists.

### [LOW] brief: Seven tasks are sized at 55 to 60 minutes, about double the sub-agent target
- Where: `docs/plans/briefs/CHUNK7_TASKS.md`:404
- Scenario: c7-search-index (model, migration, RLS, fence, indexing, Celery task, command, SRC-S11), c7-hybrid-search-backend (fused SQL, reranker, six filters, scope, snippet, find_similar, limits.py, three scenarios, budget), c7-ask-backend (streaming, grounding, citations, pending flag, ai_generation, switch, rate limit, four scenarios), c7-search-screen and c7-ask-screen (screen, feature, two journeys each), c7-eval-sets (two models, three routes, two commands, seed), c7-security-review and c7-embedder-selection-baseline are each 55 to 60 minutes.
- Fix: Split at green points: index model/fence vs apply hook (item 4 already does this); retrieval core vs find_similar+limits+budget; Ask grounding/stream vs switch+rate+log; each screen vs its journeys; eval models+commands vs routes.

## Conflicts with main

Main has moved past the plan's assumptions: (1) E4 (c7-embedder-mock-reranker) is merged at d10daf8 with RERANKER_PROVIDER/RERANKER_TOP_K, MOCK_ADAPTER_SETTINGS and the guard test, so the review-and-merge step is stale; (2) OWNER_RECOMMENDATIONS 'Alex's answers' (0b5ecf8, 6cf72f0, 3cc373a) answer q-search-fence with Option B and add items 3, 10 and 14, which the plan predates and contradicts (findings 1, 5, 6, 10); (3) H10 (18cbf5f, ee82c9c) already keeps search text out of logs and Sentry, so the plan's H10 references describe merged work; (4) H-B merged (f336cce) but H-C has not, so the 'shared or mine' mixed policy the plan copies is still the H15 shape that lets a bank session write platform rows, which item 4's split-policy shape avoids; (5) features/governance and the audit-log screen exist on main (565d946), so the c4-fe-audit-log precondition is met; the frontend split has not landed (messages/en.json and sv.json), so that precondition still holds. Resolve by revising the brief on main, not by merging this branch as is.

## Checked clean

Rule 3 (read-only POSTs through the plain client, no guard or allowlist change; rateAnswer through record()); ask takes a person's session only and findSimilar an API key with search:read or a proposals.review platform session (both scopes and permissions exist in permissions.py); D-07 prompt-content test and the one logging wrapper; H7 owner column plus forced RLS on the index; every SRC scenario except S7 (chunk 13) and S13 (f03-T73) has exactly one integration owner and one journey owner; AUD-S4 and ADM-S4 halves split across named tasks; POST /eval/runs deferred to chunk 14 and saved searches to chunk 13 with pending-file owners; no scenario-stub task needed (stubs verified skipped/fixme on main); generated files never committed by tasks; append ledgers named; search over POST; rate limits and thresholds as settings via apps.identity.rate_limit.enforce (exists); library/reading.py scope rule reused (exists); wave 3 to 7 owned paths disjoint apart from declared ledgers; SRC-01/02/03/05, AUD-02, ADM-02 (evaluation) and J-7 each have tasks and no task lies outside the chunk beyond the switch's TEN-01 touch; c7-chunk-close proves no 501 and no fixme; the plan's CLAUDE.md §11 propagation to UI tasks.