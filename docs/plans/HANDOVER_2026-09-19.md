# Handover, 2026-09-19 (written for the next session, whichever model runs it)

Read `CLAUDE.md`, then this file, then `docs/plans/IMPLEMENTATION_STATUS.md`.

## Where the build stands
- `main` = da0a5e7: Phase 0 complete and CI green; every package at its newest release (ADR 0025).
- Uncommitted on disk (large, intentional, from parallel sub-agents): chunk 1 backend and
  frontend (identity, tenant admin; 24 scenarios un-skipped, J-1 and J-8 green), the chunk 1
  security review with fixes (`docs/security/CHUNK1_AUTH_REVIEW_2026-09-19.md`), the chunk 2
  backend and frontend (in progress when this was written), the design cards for chunks 2 to 7
  (`design/screens/`), `docs/plans/UI_Implementation_Plan.md`, the prototype data fixture
  (`backend/apps/library/fixtures/`), the evaluation sets (`backend/eval/`), the watch-sweeper
  agent definition (`agents/`), and the shell and prototype logo change (phonetic mark only,
  200 px rail, no scrollbar).
- The briefs each agent worked from are in `docs/plans/briefs/` (Phase 0, chunks 1 to 4).
  Chunk 3 and 4 briefs are written; their agents were not started yet.

## What was in flight (check the working tree before assuming any of it finished)
1. Chunk 1 backend follow-ups: E2E-only sign-count tolerance, rate limits off under `E2E_MODE`,
   `GET /reference/languages`, `locale=en` on seeded logins, step-up on passkey add and remove
   with a passkey sign-in counting as fresh (security finding F9), code mail via a Celery task
   after commit with a cache-backed mock outbox (F12), `Referrer-Policy: no-referrer`.
2. Chunk 2 backend: `apps/taxonomy`, `apps/proposals` (minimal proposal plus apply for the
   vocabulary kinds), `Jurisdiction`, vocabularies seeded with the fixture's keys, footprint
   change requests with preview, four eyes and step-up. Its report names a tenant-creation hook
   in `apps/taxonomy/tenant_hooks.py` that must be called where a tenant is created.
3. Chunk 2 frontend: `/admin/vocabularies`, `/admin/vocabularies/[list]`, `/admin/footprint`,
   the `VocabularyPicker`, journeys VOC-S*, FP-S*, J-5, J-6, F28 referrer fixes, and the switch
   from hand-written identity and tenant-admin types to `src/types/api.generated.ts`.

## How to finish and ship chunks 1 and 2
1. Run the full Appendix D checklist in `CLAUDE.md`: backend first, then `bash generate-types.sh`
   from the root with `ENVIRONMENT=ci`, then the frontend gates, then `npm run test:e2e` from
   `frontend/` (the boot script `tests/e2e/support/start-backend.sh` migrates
   `compliance_watch_e2e` from zero, seeds, starts a worker and serves as `cw_app`).
2. Fix what fails; never lower a gate. Vocabulary keys follow the fixture (`six_months_plus`).
3. Update `docs/plans/IMPLEMENTATION_STATUS.md` (chunks 1 and 2 rows), commit to `main` with a
   playbook 13.4 message (no model names, no co-author line), push, watch `gh run list` until CI
   and CodeQL are green, fix what CI catches (it runs CodeQL, Trivy at MEDIUM and osv-scanner).
4. If a screenshot baseline changed, regenerate the Linux one inside
   `mcr.microsoft.com/playwright:v1.63.0-noble` (copy `frontend/` without `node_modules` to a
   temp folder, `npm ci`, `E2E_SKIP_BACKEND=1 npx playwright test pills.gallery --update-snapshots`,
   copy the `*-linux.png` back).
5. Give Alex the five-line chunk summary (what a user can do, what was cut, what landed in
   `TODO_FOR_alex.md`, which defaults were relied on, the next chunk), then continue: chunk 3
   (`docs/plans/briefs/CHUNK3_BRIEF.md`) and chunk 4 in parallel where files do not collide, a
   security review agent per chunk before its commit.

## Working agreements learned this session
- Alex: maximise parallel sub-agents partitioned by directory ownership; speed with quality and
  security; commit straight to `main`.
- Newest release of every package (ADR 0025): Django 6.x; TypeScript 7 native for `tsc` with the
  `@typescript/typescript6` package under the `typescript` name for tools; MEDIUM findings block;
  two-stage Alpine images with no Poetry, pip or npm in the runtime.
- The E2E backend must run the venv interpreter directly (never `poetry run`) or Playwright
  hangs on Windows; `NEXT_PUBLIC_API_URL` must be `localhost`, not `127.0.0.1` (SameSite cookie).
- Logo: phonetic wordmark only, about 104 px wide in a 200 px rail, scrollbar hidden.
- Docker Desktop on this machine: if the engine will not start, rename
  `%LOCALAPPDATA%\Docker\run` and relaunch (stale socket file).
