# Hardening follow-ups found in review

Findings from the security reviews of 2026-09-19 that did not block their own merge, because
each existed before that branch or belongs to a later task. Each gets built, not forgotten.
The main agent adds a row here whenever a review turns up such a finding.

| # | Finding | Found in | Severity | Where it gets fixed |
|---|---|---|---|---|
| H1 | `PageQuery.offset` has no upper bound: a huge offset makes the database raise, and every paginated route answers 500 | chunk4-T3 review | low | Task H-A below |
| H2 | `build_principal` (`apps/identity/session_logic.py`) trusts the permissions stored on a tenant role row without limiting them to `TENANT_PERMISSIONS`; seeds and the role editor refuse `proposals.review`, but a row written around them would carry it | chunk4-T11 | low, defence in depth | Task H-A below |
| H3 | Nothing stops a future writer from recording a tenant-derived title under a library subject with no tenant, which `GET /audit-events` would show every tenant | chunk4-T3 review | low, future risk | Task H-A below: a guard test listing every `record(tenant_id=None, ...)` with a library subject type and its actor type |
| H4 | `cw.maintenance` rests on review alone | chunk3-rest-T1 review | low | chunk3-rest-T1 (compliance rule, in progress) |
| H5 | Seeded library-list rows get no audit row, and every deploy undoes an approved reorder, retire or default change | chunk4-T2 and its review | medium | **Fixed** before 2026-09-21 (on `main` since the history's root, 9391b39; verified 2026-09-24 by `r1-close-and-readiness`): the library seed creates a missing row with `get_or_create` and a `record()` row and never resets an existing one. Proven by `taxonomy/tests_vocabulary.py` `test_an_approved_library_list_reorder_retire_and_default_survive_the_next_deploy` and `test_a_deploy_puts_a_library_system_rows_kind_back_and_records_it` |
| H6 | Under the append-only trigger a machine translation cannot be confirmed in place (INV-05, INV-S6) | chunk3-rest-T1 review | low, design | TODO_FOR_alex with a default; built with the screens that confirm (chunk3-rest-T12/T14 or chunk 4) |
| H7 | Child tables of tenant-private library records (titles, versions, summaries, terms, tags) have no RLS of their own; every read must reach them through the parent | chunk3-rest-T4 | low, future risk | Chunk 7's search index must carry the owner and its own RLS; the chunk 7 plan names it |
| H8 | Approval's audit rows carry `steppedUp: false` because `apply.py` does not pass the step-up id | chunk4-T3 review | low | chunk4-T6 (already in its task text) |
| H9 | The obligations count evaluates `taxonomy_in_footprint` for every visible obligation, so the cost grows with the whole library, not the page (about 200 ms per 1,000 obligations) | chunk3-rest-T4 review | low, future risk | A set-based anti-join replacing the per-row function call; before the library passes a few hundred obligations |
| H10 | Gunicorn's default access log writes every query string (`?q=` search text), client IP and user agent to stdout, and Sentry turns those lines into breadcrumbs whose message is not scrubbed | chunk3-rest-T4 review | high | **Fixed** before 2026-09-21 (`wt/log-no-query`, on `main` since 9391b39; verified 2026-09-24 by `r1-close-and-readiness`): `docker-entrypoint.sh` gives gunicorn an access-log format with no query string, address, referrer or agent. Proven by `shared/tests_no_query_in_logs.py` `GunicornAccessLog` |
| H11 | The app role can set `cw.maintenance` itself (in any letter case, since PostgreSQL setting names are case-insensitive), which switches every append-only trigger off; only a source lint stands in the way | chunk3-rest-T1 verification | medium | **Fixed** before 2026-09-21 (Task H-B, shared 0003, then shared 0005's allowlist from the H-C review; on `main` since 9391b39; verified 2026-09-24 by `r1-close-and-readiness`): the hatch opens only for the migrator as `session_user`. Proven by `shared/tests_append_only.py`, both spellings as `cw_app` |
| H12 | The `cw.maintenance` lint is case-sensitive; `LIBRARY_DIFF_MAX_SENTENCES` accepts 0, negatives and huge values (500 takes about 7 s per diff); library texts have no length limit, so a 200 KB text of short sentences costs about 0.5 s in version_diff | chunk3-rest-T1 verification | low | Task H-B below |
| H13 | Sessions carry platform grants even inside a bank, and a bank invitation does not refuse an address holding a platform role | chunk4-T4 review | medium | **Fixed** before 2026-09-21 (Task H-A, on `main` since 9391b39; verified 2026-09-24 by `r1-close-and-readiness`): a bank session reads no platform grant and a bank invitation refuses a platform address (`invitation_logic.holds_a_platform_role`). Proven by `shared/tests_hardening.py` (H2 and H13 blocks) and ID-S27 |
| H14 | Every deploy resets sort order, active and default on each tenant's own system list rows (`taxonomy/tenant_hooks.py` uses update_or_create), undoing a tenant admin's reorder | scope T01 review | medium | **Fixed** before 2026-09-21 (the T01 integration, on `main` since 9391b39; verified 2026-09-24 by `r1-close-and-readiness`): `taxonomy/tenant_hooks.py` uses `get_or_create` and puts back only a system row's kind, with an audit row. Proven by `taxonomy/tests_vocabulary.py` `test_a_tenants_reorder_retire_and_default_survive_the_next_deploy` and `test_a_deploy_puts_a_system_rows_kind_back_and_records_it` |
| H15 | `rls_operations(mixed=True)` gives `api_key`, `problem_report`, `ai_generation` and `outbox_event` one FOR ALL policy whose write check equals the mixed read rule, so a bank session can insert, change or delete platform rows at the database level (for example the platform agent's API key); two zones must hold in the database, not only in code | E5 fix, 2026-09-19 | high | **Fixed** 2026-09-20 (Task H-C, shared 0004, on `main` since 9391b39; verified 2026-09-24 by `r1-close-and-readiness`): every mixed table has a FOR SELECT mixed read and a FOR ALL own-zone write. Proven by `shared/tests_rls.py`, which also refuses a new mixed table without the split |
| H16 | `instrument` and `obligation` are mixed on `owner_tenant_id` and still check every write with the read rule (`library_fence=True` in `rls_operations`), so at the database level a bank session can change or delete a shared library record; what stands in the way is the Python fence alone (`library_write()` and its AST guard, PRO-01) | H-C | medium | **Fixed 2026-09-23** in `h16-library-db-policy` (ADR 0058, D-83), by a second layer rather than the split: shared 0008 puts one statement-level trigger on every library-zone table, not only these two, which refuses the app role's INSERT, UPDATE, DELETE and TRUNCATE unless the transaction-local `cw.library_door` names a door that table accepts — `proposal` or `seed` for the inventory and the vocabularies, `reverification` also on `obligation` and `verification`, `watch` for the seven watch tables only, `index` for `search_chunk` only — and lets the schema owner through by `session_user`. `library_write()`, `watch_write()` and `index_write()` set it; the guard in `apps/shared/tests_library_db_guard.py` enumerates the tables from the app registry and proves the refusal as cw_app in a bank's zone. The two policies keep the read rule as their write rule: the trigger stands in front of them. The integration review's follow-ups (2026-09-24) close three more gaps in the same package: `language`, `jurisdiction` and `jurisdiction_label`, shared reference rows the Python fence never saw, carry the trigger with the `seed` door alone and their seeds open it; the guard files and approves every `ProposalKind` and calls every watch step as cw_app, with a census on each, so a writer naming the wrong door fails the backend suite and not only E2E; and the library fence keeps `seed`, `library_write()`'s default and widest door, to the reference seeds whether a call names it or takes it by default. What it does not stop (code that sets the setting itself) is in the ADR |
| H18 | `ProposalDoorGuard` (`apps/shared/tests_library_fence.py`) does not know the watch door, so the first route that writes a watch table fails it. `c5-contract-models-watch` built `watch_write()` and allowlisted `watch/curation.py` as a caller, but `LIBRARY_WRITING_ROUTES` still names `approveProposal` and `reverifyObligation` alone, and `test_a_route_reaching_a_library_write_needs_proposals_review_a_step_up_and_a_person` still demands `proposals.review`, a step-up and a person's session of every route that reaches a library write. The four curation routes reach `watch_write`, are gated by `require_change_writer` (a key with `changes:write`, or a session with `proposals.review`) and take no step-up, which is the design ruling H settled — so five assertions fail and no watch write route can pass CI until the guard is taught the second door. Nothing is unsafe: the watch door still refuses every table outside the seven at runtime, and no key scope names an inventory table | c5-watch-curation | blocks every watch writer | **Built 2026-09-21** in `wt/h18-watch-fence`, recorded as D-64. The guard now keeps two maps. `LIBRARY_WRITING_ROUTES` is unchanged and still means the inventory: approveProposal through `apply.apply`, the re-verification stamp through `apply.apply_reverification`, each needing `proposals.review`, a step-up and a person's session. `WATCH_WRITING_ROUTES` names the six change routes — createChange, addChangeDocument, updateChange, addChangeEvent, updateChangeEvent, replaceChangeObligations — with `("apps.watch.write", "watch_write")` as the only writer any of them may reach. Those six are exempt from the `proposals.review`-and-step-up assertion and asserted instead to demand `changes:write` of an agent's key, through `require_change_writer` or the same scope on the key-only document route, and to reach the watch door and nothing else; each one's view has to live in `apps/watch/api.py`, so an inventory route cannot be moved into the exemption; `apply` and `apply_reverification` stay unreachable from every one of them. The scope claim is proven, not stated: the test reads the constants `require_change_writer` itself names out of the index and checks them against the table of every library model behind the proposal door. Two new tests feed the rule fabricated routes — one reaching `apply`, one with no change-writer gate — and both are red against the pre-H18 rule. **A source or a "So what?" route that opens the same door still fails this guard**: `c5-watch-sources-coverage` and `c5-ai-log-and-so-what-draft` add their own routes to the map deliberately, with their own gates (`sources.manage`, `sources:write`) looked at, which is the fail-closed behaviour the map is for |
| H17 | `watch/logic.py` no longer exists, and the library fence, PLAYBOOK 14 and `docs/CONVENTIONS.md` now name `watch/write.py` instead. Five files still name the deleted module: `backend/apps/shared/tenancy.py` (the `LibraryWriteRefused` message), `backend/apps/shared/tests_compliance_lint.py` (a planted fixture path, harmless but stale), `docs/plans/PARALLEL_PLAN.md` (the `watchwrite` key and ruling 16), `docs/plans/briefs/CHUNK7_TASKS.md` and `docs/plans/briefs/FEATURES_0_3_TASKS.md` (lines 471 and 491, which still give `f03-T41` and `f03-T43` that file). Each is only a name in prose, so nothing is unsafe; a reader following one would look for a module that is not there | c5-contract-models-watch | low | `c5-chunk-close` corrects the two brief files; the refusal message and the lint fixture go with the next task inside `apps/shared/` that may edit them, and `PARALLEL_PLAN.md`'s rows are historical. **Code half done 2026-09-23** in `h16-library-db-policy`: the `LibraryWriteRefused` message names `watch/write.py`, and the lint fixture plants its upper-case spelling in `apps/watch/curation.py` |
| H19 | A calendar feed keeps working for a person whose account status is `deactivated`, which `session_logic.resolve_access_token` refuses a session for; `feed._subscription_ended` checks the membership only. No production code sets that status yet (security-review-c6, L1) | chunk 6 review 2026-09-23 | low, latent | The task that first deactivates an account: `_subscription_ended` stops the feed on the same status, with a test beside `TheServerStopsASubscriptionItself` |
| H20 | `GET /calendar/feed.ics` is limited per token prefix only, so a client presenting a new random prefix each time is never throttled: one indexed read per request, unbounded (security-review-c6, L2) | chunk 6 review 2026-09-23 | low | A per-address bucket (`CALENDAR_FEED_RATE_PER_IP_PER_MINUTE`, env override) taken before the prefix one, with a test that a run of unknown prefixes from one address answers 429 |
| H21 | The weekly briefing mails are sent synchronously inside the snapshot transaction and the hourly beat matches one hour a week: a relay failure rolls the snapshot back and the week is never sent (the next matching hour snapshots the following week), and a failure part-way has already delivered mails a retry would repeat (security-review-c6, L3) | chunk 6 review 2026-09-23 | low, availability | Deliver through the worker on commit as identity mail does (F12), and let `send_weekly_briefings` pick up a bank's last unsent week rather than only the one that just ended |
| H22 | The roadmap keeps an item while `key_date >= today`, and the day a month- or quarter-precision date is stored on is whatever the proposer supplied, so such an item can leave the roadmap and Today while its month or quarter is still running (security-review-c6, L4) | chunk 6 review 2026-09-23 | low, correctness | `roadmap._cases` compares the end of the stated period (or the watch door stores a fixed day of it), with a test on a quarter-precision date inside its quarter |
| H23 | A library vocabulary merge rewrites the current link rows in place (`proposals/apply.py` `_repoint_rows`, `watch/write.py` `repoint`: twin rows deleted, the rest updated) and its `vocabulary.merged` audit row keeps only a count per table, so which records carried the merged-away value, and which duplicate links were dropped, cannot be rebuilt from the log; the tenant side (`taxonomy/repoint.py` `tenant_tag`) is the same (security-review-c3-f03, M2) | chunk 3 and f03 review 2026-09-23 | medium, audit trail | **Fixed 2026-09-23** in named fix task `merge-moved-ids` (below), commit 580f80d: `taxonomy/repoint.py` answers, per table, the ids a merge moves and the ids of the twins it drops (`Moves`), for the preview and the commit alike; `move()` locks the rows as it reads their ids and hands each door exactly those rows, so the ids name the rows that changed, and the doors themselves are unchanged (H16 guard and census green). The `vocabulary.merged` audit row, library and tenant, keeps its counts and adds `rows` = `{table: {moved: [ids], dropped: [ids]}}` in the same transaction: ids only, never content. Tests: `tests_apply.LibraryMergeRepoints` (a watch table and an inventory table, each with a dropped twin) and `test_voc_s5` (a tenant tag merge with a twin), each rebuilding the moved and dropped ids from the audit row, red on the base with `KeyError: 'rows'` |
| H24 | A regulatory scope change has no cap on how many terms `adds` and `removes` name or on the note's length: each term costs two lookups, a link row and, on approval, a history and an audit row, bounded only by the body size limit (security-review-c3-f03, L1) | chunk 3 and f03 review 2026-09-23 | low | `max_length` from a setting (`FOOTPRINT_CHANGE_MAX_TERMS`, env override) on both lists and a fixed cap on `note`, stated in the field descriptions, with a 422 test over the cap |
| H25 | `cw_provision_not_under_standard` (library 0008) reads the instrument's level without a lock, so a provision insert and a level merge that moves its instrument onto a standard level, committed at the same moment, can each pass the check (two platform approvals racing; security-review-c3-f03, L2) | chunk 3 and f03 review 2026-09-23 | low | **Fixed 2026-09-25** by `x-hardening-proposals`: `apply._new_provision` locks the instrument and reads it again before the standards check, and `repoint.move` already locks every instrument a level merge moves (`FOR UPDATE`). Proven by `proposals/tests_apply.py` `ApprovalsOnOneRowRace`: a merge waiting on a provision insert ends in `provision_not_under_standard`, and an insert waiting on a merge answers 422 `licensed_text`, neither leaving a provision under a standard |
| H26 | Source links render as `href` without a scheme check (`ObligationPanels`, `InstrumentScreen`, `ProvisionTree`, `ChangeScreen`, `ChangeDocuments`, `ProposalDetailScreen`, `AiLogScreen`); only proposal creation requires https, the watch pipeline accepts http and seeds are unchecked. React 19 blocks `javascript:` URLs, so nothing runs today (security-review-c3-f03, L3) | chunk 3 and f03 review 2026-09-23 | low, defence in depth | One `externalHref()` in `shared/` that renders a link only for `https:`/`http:` and plain text otherwise, used by all seven, with a vitest case for `javascript:` and `data:` |
| H27 | The inventory's string filters (`instrument`, `dutyType`, `regime`, each `term[]` item) carry no length limit; the ORM binds them, so there is no injection, only an unbounded input at a trust boundary (security-review-c3-f03, L4) | chunk 3 and f03 review 2026-09-23 | low | `max_length=80` like the slugs, stated in the descriptions, with a 422 test |
| H28 | The jurisdiction dimension row itself is outside the mirror rule: a `term_dimension` relabel proposal can change `jurisdiction`'s `restricts_footprint`, which the seed does not put back, and merging a `term_dimension` moves none of its terms while retiring it; either merge side also accepts a retired target (security-review-c3-f03, L5) | chunk 3 and f03 review 2026-09-23 | low | Refuse a proposal on a mirrored dimension at propose and apply (`jurisdiction_term_mirrored`), refuse a merge on a list that has usage but no links, refuse a retired target (`invalid_transition`), each with a test |
| H29 | Two concurrent unwatches of one market both write `markets.watch_removed` (`markets_logic.unwatch` finds, deletes, then records) (security-review-c3-f03, L6) | chunk 3 and f03 review 2026-09-23 | low, audit | Record only when `delete()` removed a row, else 404, with a race test beside `TenantListRaces` |
| H30 | The H16 proofs are per module, not per function: `registration._merge` and `add_document`, `curation.update_event`, `set_obligation_links` and `confirm_curation`, `sources.update_source` and `record_check` (so `source_check`), and `write.upsert` never run as cw_app in the backend suite; and the shared-row census exempts any model with a `tenant` field, nullable included, and looks only inside `LIBRARY_ZONE_APPS` (security-review-c3-f03, L7) | chunk 3 and f03 review 2026-09-23 | low | The watch census lists every function of an allowlisted module whose body calls `watch_write` (the fence's AST visitor) and requires each proven; the shared-row census exempts only a `TenantModel` or a table with forced RLS, and names every other platform table explicitly |
| H31 | No test proves the watched view (`footprint=watched`) empty for a second bank that watches nothing; the filter is right (by `tenant_id` and under RLS), the proof covers one bank (security-review-c3-f03, L8) | chunk 3 and f03 review 2026-09-23 | low, test gap | Tenant A watches `dk`; tenant B's `/obligations?footprint=watched` and `/instruments?footprint=watched` answer empty lists under `as_app_role()` |
| H32 | Query counts that grow with the data: `repoint.tenant_tag` runs one statement per tagging, `list_of_lists` two counts per list (about 40 queries); `GET /vocab/{list}` is unpaginated while tenant lists grow without a cap (security-review-c3-f03, L9) | chunk 3 and f03 review 2026-09-23 | low, performance | Bulk update after excluding twins as the library side does, one aggregate per list, and a row cap per tenant list from a setting |
| H33 | Agent independence is checked by definition and key, never by the person behind them: one platform admin holding `agent_definitions.manage` can mint a `proposals:write` key for one definition and a `proposals:review` key for another and alone push any agent-confirmable kind into the library; a person holding `library_vocab.manage` and `agent_definitions.manage` can propose as themself and approve with a key they minted; `create_agent_key` checks neither the agent's kind (`review`) nor that it is active, and allows both scopes on one key (security-review-c4, M5) | chunk 4 review 2026-09-23 | medium, privileged insider | A named task after Alex answers (TODO_FOR_alex, security-review-c4): `create_agent_key` gives `proposals:review` only to an active agent of kind `review` and never beside `proposals:write`; `require_reviewer` refuses an inactive agent's key; `_decidable` refuses a key decision when the key's `created_by` is the proposing person or the proposing key's creator; each with a test in `tests_decide`. **Accepted by Alex, 2026-09-24 (D-89)**: asked whether two agents' keys must come from two people, he answered "Current state is fine"; independence is checked by definition and key, platform admins are trusted and the audit trail names who minted each key. The rule above is the design if it is revisited |
| H34 | Vocabulary and term apply read the row without `select_for_update` and save the whole row, so two approvals of different proposals on one row at once can lose the other's provenance stamp or version bump (security-review-c4, L1). L15, reviewed before `new_instrument` and `new_obligation` joined `AGENT_CONFIRMABLE_KINDS`, read their absence as a gap; their presence is deliberate under D-79 (both records carry `verified_origin` and `verified_by_agent`), and the provision kinds are the ones a test must pin refused | chunk 4 review 2026-09-23 | low | Lock the row in `apply._row` and `merge_pair` (both merge rows in id order), with a `TransactionTestCase` race of an agent's and a person's relabel beside `ObligationVersionRaces`. L15 is **fixed 2026-09-24** by the wave 4 integration: `tests_kinds.NewProvision.test_an_agent_cannot_approve_a_provisions_new_text_through_the_route` pins that an agent approving `new_provision_version` through the route answers 409 `person_review_required` and applies nothing, beside the same class's refusal of `new_provision`. **H34 itself fixed 2026-09-25** by `x-hardening-proposals`: `apply._row`, `_term_update` and `logic.merge_pair` (at apply, both rows in id order) lock the row they save. Proven by `proposals/tests_apply.py` `ApprovalsOnOneRowRace.test_an_agents_and_a_persons_relabel_of_one_row_at_once_both_land_in_turn` |
| H35 | Proposal input at the trust boundary: `sourceUrl` is an https link only for new-record kinds (an `http:` link on a version, vocabulary or term proposal is rendered as its source); payload texts have no length cap, so a label over its 200-character column is queued and fails at approval with a 500; a free-text `sourceLabel` lands on a standard's obligation unchecked; a correction keeps the proposer's source when it changes the sourced value (security-review-c4, L2 to L5) | chunk 4 review 2026-09-23 | low | `logic.create` refuses a non-link `sourceUrl` for every kind; `PROPOSAL_TEXT_MAX_CHARS` and a label cap at the column width, settings with env overrides, in `validated_labels` and `_validate_text_payload`; the standards check takes the source label; a correction that changes a sourced value names a fresh source. **Fixed 2026-09-25** by `x-hardening-proposals` (the label cap in `validated_labels` is `c8-vocab-lists-rules`'): a non-https `sourceUrl` is 422 on every kind, `PROPOSAL_TEXT_MAX_CHARS` bounds texts and summaries, a standard's `sourceLabel` is its official reference or empty (`licensed_text`), and a correction that changes a sourced value names a fresh source in `fieldSources` (D-9x). Proven by `proposals/tests_create.py` `ProposalInputAtTheBoundary` and `tests_apply.py` `test_a_correction_that_changes_a_sourced_value_names_a_fresh_source` |
| H36 | `agents/runs.require_open_run_of_key` reads the run without a lock, so a decision can land in a run another request is closing and the run's counts miss it (security-review-c4, L6) | chunk 4 review 2026-09-23 | low | **Fixed 2026-09-25** in `x-hardening-agent-runs`. `runs._own_run` locks the run's row (`select_for_update(of=("self",))`); its callers are the close and every filing that names a run (`require_open_run`, `require_open_run_of_key`), and `finish_run` reads, counts and closes inside one transaction under that lock. A close waits for a filing in flight and counts it, a filing that waited on a close reads the run closed (`run_not_open`), and of two different closes at once one lands and the other answers `invalid_transition`. Tests: `agents/tests_runs.ARunLocksOnWrite`, red before the fix |
| H37 | The audit read's row rule stands in Python alone: `audit_event`'s library read policy is `tenant_id IS NULL`, so under a bank's session the database returns another bank's `proposal.rejected` title and note; the rule reads the actor's platform role now, so revoking an editor's role hides their library changes from every bank; and the H3 guard fingerprints the title only (security-review-c4, L7 to L9) | chunk 4 review 2026-09-23 | low, defence in depth | A migration narrowing `library_rows_visible` on `audit_event` to library subjects, pinned in `LIBRARY_READ_RULES` with a `tests_rls` probe as `cw_app`; an actor-zone column stamped by `record()` and read by the rule; the H3 fingerprint adds `summary`, `before` and `after` |
| H38 | `POST /me/visit` has no throttle, and each call appends an audit and an outbox row kept for ten years; a bank's name refuses no control or bidi character, and a newline makes the invitation mail raise in the worker, so the first admin is never invited (security-review-c4, L10 and L11) | chunk 4 review 2026-09-23 | low | `VISIT_MIN_INTERVAL_SECONDS` (env override): a visit inside it writes nothing; tenant create and update refuse Unicode categories Cc and Cf with 422 |
| H39 | Problem reports: a report with no tenant is refused only by a type hint while the table keeps `library_rows_visible`; `versionNumber` above 2^31 and a NUL character in a description or a note answer 500; filing and closing have no rate limit (security-review-c4, L12 to L14) | chunk 4 review 2026-09-23 | low | `create_report` raises on a missing tenant (and the column becomes NOT NULL or the read rule `false`, per Alex's open TODO); `le=` on `versionNumber` and a shared refusal of `\x00` on write bodies; a per-person bucket with ACC-09 (R2) |
| H40 | Nothing screens a proposal's title, payload texts or `fieldSources` for embedded instructions (AGT-07), so fetched text an agent copies into a correction reaches the review queue, and an agent reviewer, unflagged; and a risk flag gates nothing, so a machine confirmer may confirm a change whose page is flagged (security-review-c5, M2) | chunk 5 review 2026-09-23 | medium | **Fixed 2026-09-23** in `agent-write-guards`, commit 404f22f. `proposals.logic.create` screens the title, every string of the parsed payload, each field source, `sourceLabel` and `sourceUrl` with `agents/screen.py` (`screen_all`), stores the text as it arrived and the findings in `proposal.risk_flags` (proposals 0007), names them on the audit row as `riskFlags` and returns them on every proposal row; the console queue shows a warning "Flagged" pill and the detail a notice. An agent's approval of a flagged proposal, or an agent's correction that brings flagged text in, answers 409 `risk_flagged`; so does an agent's curation confirmation of a change any of whose pages carries a risk flag (F9). A person still decides either, seeing the flag; an agent may still reject. Tests: `proposals/tests_screening.py`, `watch/tests_curation.ConfirmingCuration.test_no_agent_confirms_a_change_whose_page_is_flagged_and_a_person_still_may`, the queue presentation and detail tests, each red before the fix |
| H41 | The sweeper's per-run budgets (`max_proposals_per_run`, `max_rechecks_per_run` in `agents/watch-sweeper/v1/definition.yaml`) are enforced by nothing on the server, so a runaway or injected run can file unbounded proposals and re-check lines under one open run (security-review-c5, M3) | chunk 5 review 2026-09-23 | medium | **Fixed 2026-09-23** in `agent-write-guards`, commit 65cdeac. `WATCH_RUN_MAX_PROPOSALS` (50), `WATCH_RUN_MAX_CHANGES` (50) and `WATCH_RUN_MAX_RECHECKS` (100), the definition's write budgets, are settings with env overrides that refuse to boot below 1. `runs.spend` locks the run's row, re-reads that it is open, counts what the run filed and refuses one more with 422 `run_budget_exhausted` before anything is stored: `proposals.create` for a new proposal (a replay is not counted), `registration.register_change` for a new change (a second sighting is not counted) and `sources.record_check` for a re-check line (a sweep is not counted). A close stores the server's own count of `sourcesChecked`, `changesRegistered`, `proposalsSubmitted` and `recordsRechecked`, whatever the run sent, and a replayed close compares on the same counts; model calls, fetches, `outOfScope` and `correctionsProposed` stay the run's account, since the server cannot see them. The model-call and fetch budgets are the runner's to hold. Tests: `agents/tests_budgets.py`, red before the fix |
| H42 | A key's `PATCH /changes/{id}`, milestone and obligation-link writes name no run, so a "So what?" filed by PATCH is logged with none; and a key bound to no agent stores its suggestion with no suggester, so a later confirmation names only the confirmer (security-review-c5, L6) | chunk 5 review 2026-09-23 | low | An `agentRunId` required of a key on those four routes and checked with `runs.require_open_run`, passed to `so_what_draft.store`; `require_change_writer` refuses a key bound to no agent with `agent_not_bound`, as open_run does |
| H43 | A draft or inactive agent definition is minted working keys and deactivating one leaves its keys working (both shipped definitions are drafts); a platform key's scopes are not tied to its agent's kind, so a review agent's key could hold `proposals:write` (security-review-c5, L5; the kind and active checks on `create_agent_key` overlap H33) | chunk 5 review 2026-09-23 | low | **Fixed 2026-09-25** in `x-hardening-agent-runs`, with D-93. `create_agent_key` refuses a definition that is retired or switched off (422 `agent_inactive`) and a scope its kind does not take (422 `scope_not_for_kind`: a review agent holds `proposals:review` and no filing scope, every other kind the filing scopes and never `proposals:review`); `resolve_api_key` resolves no key whose agent is so, so its keys answer 401 at once. A draft, read from its shipped definition file, keeps both (D-93), because both shipped definitions are drafts and J-4 and PRO-S13 mint their keys. Tests: `identity/tests_api_keys.KeysFollowTheirAgent`, red before the fix |
| H44 | The watch writes accept `Idempotency-Key` and ignore it; two concurrent registrations of one stable key answer 500 on the unique constraint; a proposal's replay key is global rather than per proposer, so a key reused in a later run answers the old proposal and a clash with another zone's invisible row is a 500 (security-review-c5, L8) | chunk 5 review 2026-09-23 | low | Honour the header on createChange and recordSourceCheck or drop it from the routes; register in a savepoint and merge on `IntegrityError`; scope the proposal replay to the proposer and its run. The per-proposer part is **fixed 2026-09-23** by `security-review-c4`, commit 030f36f (proposals 0006: the replay answers only the person or key that sent it, so another zone's row is never a clash); a key reused by the same proposer in a later run still answers the old proposal |
| H45 | A re-check line can name a library record that does not exist or does not cite the source, so the coverage log can show a comparison that never happened (security-review-c5, L9) | chunk 5 review 2026-09-23 | low | `sources.record_check` resolves `subjectId` by its `subjectType` and refuses `unknown_record`; the test that pins a random id is accepted is rewritten |
| H46 | An editor can switch on a `standards_body` source whose host is not in `STANDARDS_PUBLISHER_HOSTS`, where D-45 keeps standards bodies off until the lawyer answers (security-review-c5, L7) | chunk 5 review 2026-09-23 | low | `update_source` refuses `active` on a `standards_body` source until its host is cleared the way publisher hosts are, with a test; or a decision row making the editor's switch the clearance |
| H47 | Ask's limit is a rate, not a concurrency cap: each open stream holds a gunicorn thread until the model finishes (up to `LLM_DEADLINE_S`, 180 s), and the default deployment has 2 workers × 4 threads, so one member with `search.use` can hold most of the API's threads for every bank, and two can hold all (security-review-c7, L1) | chunk 7 review 2026-09-23 | low, availability | `r1-perf`: a per-person cap on open Ask streams (`ASK_STREAMS_PER_USER`, env override, refused below 1) taken beside `limits.ask_bucket` and released when the stream closes, with a test that a second concurrent stream past the cap answers 429 before any byte; or a capacity note in the deploy runbook if the owner prefers threads |
| H48 | `rateAnswer` finds an answer by its bank, not its asker, so a member with `search.use` who knows a colleague's answer id (the AI log shows it) can replace that colleague's verdict and note; each change is audited (security-review-c7, L2) | chunk 7 review 2026-09-23 | low, integrity | The next task in `apps/search/ask.py`: `ai_log.answer_of` also matches `asker_id`, so another member's answer is `not_found`, with a test beside `test_another_banks_answer_is_not_found_and_stays_unrated` |

## Task H-A: three small guards (after chunk3-rest-T3 merges, which owns `apps/shared/schemas.py`)

**Owned:** `backend/apps/shared/schemas.py` (the `PageQuery.offset` bound only),
`backend/config/settings.py` (one labelled setting), `backend/.env.example`,
`docs/runbooks/RAILWAY_VARIABLES.md`, `backend/apps/identity/session_logic.py` (the principal's
permissions only), `backend/apps/shared/tests_hardening.py` (new).

1. H1: bound `offset` with `le=settings.API_PAGE_OFFSET_MAX` (a setting with an env override;
   default 100000); a test that an offset above it answers 422 on a paginated route.
2. H2: the principal's permissions are the role rows' permissions intersected with
   `TENANT_PERMISSIONS` for a tenant session (and with the platform set for a platform
   session); a test that a role row carrying `proposals.review` does not give it.
3. H3: a guard test that walks every `record(` call in production code (AST), and fails when
   one passes `tenant_id=None` with a library subject type unless its actor is the system, an
   agent or a platform editor and its title is not tenant-derived; list today's calls in the
   test as the allowed set, so a new one must be looked at.

Gates: `apps.shared apps.identity apps.governance` tests, ruff, mypy, compliance lint,
requirements coverage. Security review before merge (authentication).

## Task H-B: the append-only guard the app role cannot switch off

**Owned:** a new migration in `backend/apps/shared/migrations/` that replaces `cw_append_only_guard`, `backend/apps/shared/migration_helpers.py`, `backend/scripts/compliance_check.py`, `backend/apps/shared/tests_compliance_lint.py`, `backend/config/settings.py` (the diff cap's bounds only), `backend/apps/shared/tests_audit_on_write.py` or a new `tests_append_only.py`.

1. H11: the guard function ignores `cw.maintenance` when `current_user` is the app role (compare against the role name the settings already know, never a hard-coded literal that could drift), so only the schema owner in a migration can use the hatch. Tests as `cw_app`: `SET LOCAL cw.maintenance = 'on'` and `SET LOCAL CW.MAINTENANCE = 'on'` still leave UPDATE and DELETE refused on every append-only table; the migration role can still use it inside a migration.
2. H12: the lint matches `cw\s*\.\s*maintenance|maintenance_setting` case-insensitively, with an upper-case plant in its test; the diff cap refuses to boot outside 1 to 200; a library text longer than a setting (`LIBRARY_TEXT_MAX_CHARS`, env override) is diffed as one delete and one insert, with a test.

Gates: `apps.shared apps.library` tests under coverage, ruff, mypy, compliance lint, migrate_from_zero. Security review before merge.

## Task H-C: mixed tables write only their own zone

**Owned:** `backend/apps/shared/migration_helpers.py` (`rls_operations`), one new migration per app that owns a mixed table (`api_key`, `problem_report`, `ai_generation`, `outbox_event`, and any other table the helper marks mixed), `backend/apps/shared/tests_rls.py`.

Split every mixed table's policy the way `agent_run` does since the E5 fix: a FOR SELECT policy with the mixed read rule (the tenant's rows and the rows without a tenant), and a FOR ALL policy whose USING and WITH CHECK are `tenant_id IS NOT DISTINCT FROM NULLIF(current_setting('app.tenant_id', true), '')::uuid`, so a bank session writes only its own rows and a session with no tenant writes only platform rows. Keep any narrower policy a table already has (the problem-report exception, if Alex approves it, is designed on top of this). Tests as the real cw_app role, per table: under tenant A, inserting, updating and deleting a platform row is refused or touches 0 rows, moving a row between zones is refused, and reads are unchanged. The RLS guard asserts the policy shape for every mixed table, so a new mixed table cannot get the old shape. Security review before merge.

**Added from the H-B review (low, same file):** the append-only guard's hatch check names the role to refuse (`current_user <> 'cw_app'`), so any other role, a SECURITY DEFINER function owned by the migrator, or a cascading foreign key into an append-only table could still use it. Replace it with an allowlist: `current_setting('cw.maintenance', true) = 'on' AND session_user = <the migrator role>` in a new shared migration, and add a guard test that no SECURITY DEFINER function exists and no foreign key into an append-only table has a delete or update action. The compliance lint also accepts the quoted spelling `"CW"."MAINTENANCE"`: widen its pattern to optional quotes (case-insensitive) with a planted test.

**On the search index (added 2026-09-21, from the `c7-search-index-apply` review):** the split H-C built is what makes a rebuild of the shared index impossible from a bank's own session, so `apps/search/indexing.py` is now the fourth entry in the zone-escape allowlist `apps/shared/tests_tenancy.py` keeps (`platform_zone`, `clear_tenant`), beside the tenancy module, `record()` and `log_event()`. It is recorded as D-65, with what the entry permits, why a library record's chunks need the platform zone and what still refuses. No change to any policy: the opening is in Python, above a database rule that stays as H-C left it.

**On `problem_report` (added 2026-09-20, from Alex's answer to chunk 4's Q1):** a bank's problem reports stay inside the bank, so no platform read window is built on top of H-C's split — H15's fix keeps the plain split policy for `problem_report`, with no extra policy, no column trigger and no transaction-local flag, and `chunk4-T16` pins that shape in `tests_rls.py`.

## Named fix task `merge-moved-ids` (H23, from security-review-c3-f03)

**Owned:** `backend/apps/proposals/apply.py` (`_vocabulary_merge`, `_repoint_rows`),
`backend/apps/watch/write.py` (`repoint`), `backend/apps/taxonomy/repoint.py`, their tests.

A merge moves the current rows and keeps the source row retired, so past versions still
resolve; what the log cannot answer is which current rows moved and which duplicate links
were dropped. Make the re-point return, per table, the ids it moved and the ids of the
twins it dropped, and put them in the `vocabulary.merged` audit row's `after` (the tenant
merge's too), in the same transaction.
Tests: a library merge with one twin names the moved ids and the dropped twin's id in its
audit row; the same for a tenant tag merge. Gates: `apps.proposals apps.watch apps.taxonomy`
with the guard suites, ruff, mypy, compliance lint. Security review before merge.
