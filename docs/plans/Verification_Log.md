# Verification log

Claims about the outside world that the design rests on. Re-check on each PRD
bump. Add a row whenever the build relies on something external.

| Claim | Status | Source | Checked |
|---|---|---|---|
| `@sebgroup/green-core` 3.23.0 and `@sebgroup/green-tokens` 3.1.8 are Apache-2.0. Tokens ship as CSS under `2023/css/` with light and dark both scoped to `:root` | Verified | npm package contents | 2026-09-19 |
| Green Core ships an MCP server (`green-core-mcp`) that needs `@modelcontextprotocol/sdk` installed beside it. Tools: `search_components`, `get_component_docs`, `list_guides`, `get_guide`, `get_instructions`, `get_tokens` | Verified by running it | npm package | 2026-09-19 |
| `gds-badge` variants: information, notice, positive, warning, negative, disabled. `information` is grey and `notice` is blue. No brand variant | Verified | Package type definitions and token values | 2026-09-19 |
| The prototype's colours equal Green token values (brand `#003824` = `l2-brand-01`, brass = `l3-brand-02` on `content-brand-02`) | Verified | Token files against the prototype | 2026-09-19 |
| Green components under Next.js server rendering | **Not verified** | Spike in Phase 0 (D-04) | |
| Playwright 1.61 (15 June 2026) adds `browserContext.credentials`, a virtual WebAuthn authenticator that can seed known keys, in all browsers | Verified | Playwright release notes | 2026-09-19 |
| A passkey belongs to one RP ID. Related Origin Requests: Chrome and Edge 128, Safari 18, Firefox 152 (May 2026) | Verified | Chrome for Developers, web.dev, vendor write-ups | 2026-09-19 |
| NIST SP 800-63-4 (July 2025): synced passkeys meet AAL2, device-bound needed for AAL3. AAL2 re-authentication guidance of 12 h and 30 min idle | Secondary sources only | Verify against NIST before quoting to a customer | 2026-09-19 |
| PostgreSQL: superusers and `BYPASSRLS` roles always bypass row security, table owners do unless `FORCE ROW LEVEL SECURITY` | Verified | PostgreSQL documentation, Row Security Policies | 2026-09-19 |
| pgvector HNSW indexes `vector` up to 2,000 dimensions | Verified earlier | pgvector documentation | 2026-09-18 |
| Anthropic's API `inference_geo` accepts only `us` and `global`. EU-pinned inference through Bedrock (including Stockholm) or Vertex. Managed Agents sit outside zero data retention | Parameter verified in Anthropic docs. Values and the ZDR point from secondary sources | Re-check before D-07 is contracted | 2026-09-19 |
| Claude Managed Agents: API-owned sessions, budgets, webhooks, vaults, beta header | From the Compliance Data chat | **Fetch current docs before building D-08** | 2026-09-18 |
| DORA Article 30: locations of services and data with notice of change, audit rights and exit for critical or important functions. Certifications support but do not replace audit rights | Secondary sources | Read the regulation text before drafting contracts | 2026-09-19 |
| Latest on PyPI: `webauthn` 3.0.0, `django-ninja` 1.7.0, `pgvector` 0.5.0. Django 5.2 is the LTS line | Verified | PyPI | 2026-09-19 |
| Railway: EU West region, Postgres templates with pgvector, private S3-compatible buckets | From the Compliance Watch chat | Verify in the Railway console when creating the project | 2026-09-18 |
| Embedding candidates covering sv, da, nb, fi, en at 1024 dimensions | **Not verified** | Decide against the evaluation set (D-09) | |
| Playwright 1.63.0: `browserContext.credentials` is the virtual WebAuthn authenticator (property added in 1.61); `storageState({credentials})` and `setStorageState` restore it, so a seeded passkey survives a saved state | Verified | playwright.dev, BrowserContext documentation | 2026-09-19 |
| Django 5.2.17 is the newest patch of the 5.2 LTS line. Django 6.1.1 exists; D-17 pins 5.2 (ADR 0017) | Verified | PyPI | 2026-09-19 |
| django-ninja 1.7.0, webauthn 3.0.0 and pgvector 0.5.0 are the newest releases | Verified | PyPI | 2026-09-19 |
| Next 16.3.5 and @playwright/test 1.63.0 are the newest releases | Verified | npm | 2026-09-19 |
| TypeScript 7.0.2 ships the native `tsc` and no JavaScript API; the announcement's side-by-side arrangement (`typescript` name resolving to `@typescript/typescript6` 6.0.3 for tools, `@typescript/native` carrying 7.0.2 for the CLI) runs lint (typescript-eslint 8.70.0), typecheck, Vitest, `next build` and openapi-typescript 7.13.0 green | Verified (ADR 0025) | TypeScript 7.0 announcement, devblogs.microsoft.com; the frontend gates | 2026-09-19 |
| Green Core 3.23.0 `GdsButton` renders on the server and hydrates under Next 16 `next start` with only `'use client'` on the rendering component; no dynamic import, manual registration or CSS import needed | Verified by the D-04 spike (`frontend/tests/e2e/green-spike.spec.ts`, ADR 0004) | Production build, Chromium | 2026-09-19 |
| Playwright 1.63.0 typings: the virtual authenticator is `browserContext.credentials`; helper names in `frontend/tests/e2e/support/passkeys.ts` were written against the installed `@playwright/test` type definitions | Verified | `node_modules/@playwright/test` typings | 2026-09-19 |
| Django 6.1.1 passes the whole backend checklist unchanged: 297 tests (168 skipped stubs), coverage floors, ruff, mypy with django-stubs 6.1.1, compliance, requirements, contract and search gates; django-ninja 1.7.0, django-cors-headers 4.9.0 and dj-database-url 3.1.2 declare Django 6 in their classifiers | Verified (ADR 0025) | PyPI classifiers; the backend checklist | 2026-09-19 |
| `python:3.12-alpine` and `node:22-alpine` scan clean at MEDIUM and above once Poetry, pip and npm are removed from the runtime layers; `python:3.12-slim` (Debian 13.7) carried 91 MEDIUM and HIGH OS findings with no upstream fix | Verified with Trivy locally | aquasec/trivy image scans | 2026-09-19 |
| py_webauthn 3.0.0 public API (`webauthn/__init__.py`): `generate_registration_options`, `verify_registration_response`, `generate_authentication_options`, `verify_authentication_response`, `base64url_to_bytes`, `options_to_json`; `webauthn.helpers` adds `options_to_json_dict`, `bytes_to_base64url`, `parse_registration_credential_json`, `parse_authentication_credential_json`, `parse_client_data_json` | Verified | Installed package under `backend/.venv` (webauthn 3.0.0 METADATA, BSD-3-Clause; needs pyasn1, cbor2, cryptography, pyOpenSSL) and the README at github.com/duo-labs/py_webauthn | 2026-09-19 |
| `generate_registration_options(*, rp_id, rp_name, user_name, user_id: bytes | None, user_display_name, challenge, timeout=60000 ms, attestation, authenticator_selection: AuthenticatorSelectionCriteria(authenticator_attachment, resident_key: ResidentKeyRequirement, require_resident_key, user_verification: UserVerificationRequirement), exclude_credentials: list[PublicKeyCredentialDescriptor(id: bytes, transports)], supported_pub_key_algs, hints)` returns `PublicKeyCredentialCreationOptions`; `generate_authentication_options(*, rp_id, challenge, timeout, allow_credentials, user_verification)` returns `PublicKeyCredentialRequestOptions` | Verified | `webauthn/registration/generate_registration_options.py`, `webauthn/authentication/generate_authentication_options.py` in the installed package | 2026-09-19 |
| `options_to_json(options)` is `json.dumps(options_to_json_dict(options))`; the dict is camelCase with bytes base64url-encoded: creation `{rp{name,id}, user{id,name,displayName}, challenge, pubKeyCredParams[{type,alg}], timeout, excludeCredentials[{id,type,transports}], authenticatorSelection{authenticatorAttachment,residentKey,requireResidentKey,userVerification}, attestation, hints}`; request `{challenge, timeout, rpId, allowCredentials, userVerification}` | Verified | `webauthn/helpers/options_to_json.py`, `options_to_json_dict.py` | 2026-09-19 |
| `verify_registration_response(*, credential: str | dict | RegistrationCredential, expected_challenge: bytes, expected_rp_id: str, expected_origin: str | list[str], require_user_presence=True, require_user_verification=False, supported_pub_key_algs, pem_root_certs_bytes_by_fmt)` parses a str or dict itself through `parse_registration_credential_json` (keys `id`, `rawId`, `type`, `response.clientDataJSON`, `response.attestationObject`, `response.transports`) and returns `VerifiedRegistration(credential_id: bytes, credential_public_key: bytes (COSE), sign_count, aaguid: str, fmt, credential_type, user_verified, attestation_object, credential_device_type: CredentialDeviceType(single_device | multi_device), credential_backed_up)` | Verified | `webauthn/registration/verify_registration_response.py`, `webauthn/helpers/parse_registration_credential_json.py`, `structs.py` | 2026-09-19 |
| `verify_authentication_response(*, credential: str | dict | AuthenticationCredential, expected_challenge, expected_rp_id, expected_origin: str | list[str], credential_public_key: bytes, credential_current_sign_count: int, require_user_verification=False)` parses the dict (`response.clientDataJSON`, `authenticatorData`, `signature`, `userHandle`) and returns `VerifiedAuthentication(credential_id, new_sign_count, credential_device_type, credential_backed_up, user_verified)`; `parse_client_data_json(bytes)` gives `CollectedClientData(type, challenge: bytes, origin, cross_origin)`, which is how the stored challenge of a discoverable sign-in is found | Verified | `webauthn/authentication/verify_authentication_response.py`, `parse_authentication_credential_json.py`, `parse_client_data_json.py` | 2026-09-19 |
| A challenge from `generate_challenge()` is 64 random bytes from `secrets`; failures raise `webauthn.helpers.exceptions.InvalidRegistrationResponse`, `InvalidAuthenticationResponse`, `InvalidJSONStructure` | Verified | `webauthn/helpers/generate_challenge.py`, `exceptions.py` | 2026-09-19 |
| Django removed `django.contrib.postgres.fields.CITextField` (deprecated 4.2, gone in 5.1); a custom field whose `db_type` returns `citext` is the replacement, and `ArrayField` needs `django.contrib.postgres` in INSTALLED_APPS (system check postgres.E005) | Verified | Django 6.1.1 system check output on 2026-09-19; Django 4.2 release notes | 2026-09-19 |
| PostgreSQL `FORCE ROW LEVEL SECURITY` applies policies to the table owner, so the test runner's own migrator connection is filtered too: a test writes or reads a tenant row only after `tenancy.activate()`; `SET LOCAL` changes are undone by `ROLLBACK TO SAVEPOINT` and kept by `RELEASE SAVEPOINT` | Verified | Observed in the backend suite on 2026-09-19 (the factories open a transaction and activate); PostgreSQL docs on `SET LOCAL` and savepoints | 2026-09-19 |
| OWASP ASVS: the latest stable release is 5.0.0 (30 May 2025); its chapters are V1 Encoding and Sanitization, V2 Validation and Business Logic, V3 Web Frontend Security, V4 API and Web Service, V5 File Handling, V6 Authentication, V7 Session Management, V8 Authorization, V9 Self-contained Tokens, V10 OAuth and OIDC, V11 Cryptography, V12 Secure Communication, V13 Configuration, V14 Data Protection, V15 Secure Coding and Architecture, V16 Security Logging and Error Handling, V17 WebRTC. The 4.0 numbering (V2 authentication, V3 sessions, V4 access control) no longer applies | Verified | owasp.org/ASVS ("latest stable version 5.0.0"), github.com/OWASP/ASVS releases page and the `5.0/en` chapter files at tag v5.0.0 | 2026-09-19 |
| py_webauthn 3.0.0 refuses an assertion whose sign count is not greater than the stored count whenever either is above zero (`InvalidAuthenticationResponse`), and accepts a zero counter both sides, so a cloned-authenticator regression is refused but not flagged on the credential | Verified | `webauthn/authentication/verify_authentication_response.py` in the installed package, lines 159 to 165 | 2026-09-19 |
| Railway's edge proxy strips a client-supplied `X-Forwarded-For` and writes the connecting address, and sets `X-Real-IP`; one proxy hop sits in front of a service, so `TRUSTED_PROXY_HOPS=1` reads the client from the rightmost (only) hop | **Not verified against official docs** | Railway help station threads ("Edge Proxy X-Forwarded-For and X-Real-Ip can't be trusted", "Which header should I rely on for real client IP?", June 2026); the official public-networking page says nothing about the headers. Alex confirms on the test deploy (TODO_FOR_alex.md) | 2026-09-19 |
| The community passkey provider list at github.com/passkeydeveloper/passkey-authenticator-aaguids maps AAGUID to a display name (`aaguid.json`, 56 entries, each `name` plus optional base64 SVG `icon_light`/`icon_dark`); `combined_aaguid.json` (394 entries) adds FIDO MDS entries such as the YubiKey 5 Series. The README limits use to naming passkeys in end-user passkey management UIs and warns the list may be retired to an empty object. The upstream list now names Apple's AAGUID `fbfc3007-154e-4ecc-8c0b-6e020557d7bd` "Apple Passwords" (renamed from "iCloud Keychain"); only "iCloud Keychain (Managed)" keeps the old name. Vendored names only, from `aaguid.json`, to `backend/apps/identity/data/passkey_aaguid_names.tsv` | Verified | The repository at commit 91caa531bac92f1314df430b6a92aef31907dafa (main); `aaguid.json` last changed in 49a4a4e226059e92cdc28f3572de9f9ac61d5c0b on 2026-09-07; the README; the commit log ("s/iCloud Keychain/Apple Passwords") | 2026-09-19 |
| The passkey provider list publishes **no licence**: the repository has no LICENSE file and GitHub's licence API answers 404. Only the provider names are vendored (no icons), for the one use the README names. Alex to confirm this is acceptable (TODO_FOR_alex.md) | **Not verified: no licence published** | `gh api repos/passkeydeveloper/passkey-authenticator-aaguids/license` (404) and the repository contents listing | 2026-09-19 |
| FIDO Metadata Service relying-party usage terms grant a limited, non-transferable right to download and use metadata for authentication, and forbid copying, distributing, displaying or creating derivative works from it without a separate written agreement; so the MDS-derived names in `combined_aaguid.json` (hardware keys) are not vendored, and those authenticators are named from their transport ("Security key") | Verified | fidoalliance.org/metadata-usage-terms-for-relying-parties-or-service-providers-2/ (linked from fidoalliance.org/metadata/metadata-legal-terms/) | 2026-09-19 |
| `input-otp` 1.5.0 is the newest release (npm dist-tag latest, registry modified 2026-08-18), MIT licensed (LICENSE file, Guilherme Rodz), peer React 16.8 to 19. It renders one real input behind visual slots; its own paste handler runs only on iOS or when a `pasteTransformer` is given, otherwise the browser pastes into the single input. Pinned exactly in `frontend/package.json` | Verified | `npm view input-otp` and the installed package (`package.json`, `LICENSE`, `dist/index.mjs`, `dist/index.d.ts`) | 2026-09-19 |
| `@radix-ui/react-slot` 1.3.3 is the newest release; added for `asChild` on the sidebar menu button (ADR 0025, exact pin) | Verified | npm | 2026-09-19 |
| seb.io's own navigation rail (neutral surface, hairline border, icon and label rows, a Minimise control) is the reference for the rebuilt sidebar; its active item is deliberately not copied (shadcn's small-radius row instead, Alex, 2026-09-19) | Verified by looking at the site | https://seb.io, ADR 0020 amendment | 2026-09-19 |
| shadcn/ui's current Button, Card, Badge, Sidebar and Table components and its dashboard blocks are the reference for the redesigned foundations; its Badge is fully rounded, which is why pills stay round while every other control is 6 px (`design/system/foundations.md`) | Verified | https://ui.shadcn.com/docs/components | 2026-09-19 |
| `scripts/prepush.sh` runs CI's scanners at CI's versions: CodeQL bundle `codeql-bundle-v2.27.0` (CLI 2.27.0) is what `github/codeql-action` v4.38.1 at `1c5b675` uses; that action runs `security-extended` as `codeql/<lang>-queries:codeql-suites/<lang>-security-extended.qls`, `--build-mode`, `--source-root` and `--codescanning-config`. gitleaks 8.30.1 (the action runs `gitleaks detect --redact -v --exit-code=2`, config from `GITLEAKS_CONFIG`). osv-scanner 2.6.0 (the action image `ghcr.io/google/osv-scanner-action:v2.6.0` passes the `scan-args` lines to `osv-scanner`). Trivy 0.70.0 (trivy-action v0.36.0 at `ed142fd` defaults `version` to v0.70.0 and runs `trivy image` with the inputs as `TRIVY_*` variables). Downloads are checked against each release's published SHA-256 | Verified | `src/defaults.json`, `src/codeql.ts`, `src/analyze.ts` of github/codeql-action at `1c5b675653bb5c22dbe9b12b556ec555138e09fd`; `src/gitleaks.js` of gitleaks/gitleaks-action at `e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e`; `osv-scanner-action/action.yml` of google/osv-scanner-action at `a345acffa64b0eaede81a3d9aae6141214d9c8fc` and `goreleaser-action.dockerfile`, `exit_code_redirect.sh` of google/osv-scanner at v2.6.0; `action.yaml`, `entrypoint.sh` of aquasecurity/trivy-action at `ed142fd0673e97e23eac54620cfb913e5ce36c25`; the checksum files of each GitHub release | 2026-09-19 |
