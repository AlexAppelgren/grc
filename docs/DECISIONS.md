# Decisions and defaults

The build never waits on these. Each has a default the agent uses, the reason,
and what the owner should confirm. In Phase 0 each becomes an ADR with status
`accepted by default`. When the owner changes one, the ADR is superseded and
the PRD version bumps if behaviour changes.

| ID | Decision | Default the build uses | Why | Owner confirms |
|---|---|---|---|---|
| D-01 | Product or internal tool | Multi-tenant product: one shared library, many tenants | The two-zone model works unchanged with one tenant | Before the first external tenant |
| D-02 | WebAuthn RP ID | `WEBAUTHN_RP_ID` = the exact app host per environment. Proposed production host `compliance.bleqq.com` | A passkey belongs to one RP ID. The exact host keeps other bleqq.com sites from requesting these credentials. A later rebrand means re-enrolment, or keeping this host alive and serving `/.well-known/webauthn` for Related Origin Requests | **Before any real user enrols.** Test deploys use throwaway passkeys |
| D-03 | Auth library | `webauthn` (py_webauthn) with our own thin endpoints for invitation, code and passkeys | The rules are few, specific and product invariants (code works once, no fallback, admin recovery, tenant credential policy). A framework's generic flows would be overridden more than used. SSO in R3 is decided then | Nothing now |
| D-04 | UI foundation | Tailwind + Radix primitives + our own components that reproduce the prototype, on Green tokens. A Green Core component only after a Phase 0 spike proves it renders and hydrates under `next start` and matches the design | The prototype uses Green's token values but none of its components. Green's own agent instructions require its text and layout components everywhere, which would fight the prototype and Tailwind | After the spike |
| D-05 | Brand layer | Import Green 2023 tokens, override `brand-01` and `brand-02` in `brand.css` with our own green and sand. Hanken Grotesk and Noto Sans Mono. Logo is the phonetic wordmark `[blɛkː]` | The prototype's brand green and brass pair are SEB's brand tokens by value, and Apache-2.0 grants no trademark rights. The product is sold to other banks | **Pick the replacement values.** Until then `brand.css` ships the placeholders in `design/brand/README.md` |
| D-06 | Sessions | Access token in memory, rotating refresh in an HttpOnly cookie, `user_session` row for revocation. Defaults: idle 30 min, absolute 12 h, both tenant policy | Instant revocation plus the last repo's proven refresh design | The default limits |
| D-07 | LLM provider and inference region | Adapter with `mock`, `anthropic` and `bedrock` providers. Test environment uses `anthropic`. No tenant-zone text is sent to a model except the question typed into Ask, which a tenant can switch off | Anthropic's own API offers only US or global inference. EU-pinned inference needs Bedrock or Vertex | Before the first bank tenant: contract the EU path |
| D-08 | Agent runtime | `agent_runner` adapter with a mock. First real runner: Claude Managed Agents, one session per run, the app as scheduler. Fallback: the Agent SDK inside our worker. Fetch current docs before building either | Tenant-scoped runs cannot live under a personal account. Managed Agents is in beta, so the adapter keeps it swappable | After the R2 agent chunk |
| D-09 | Embedding model and reranker | `embedder` adapter, 1024 dimensions, mock in tests. Choose the real model against the evaluation set from candidates covering all five languages | The model must be chosen on evidence. pgvector's HNSW index limit for `vector` is 2,000 dimensions | **Supply a key for the first candidate** so search works on the test deploy |
| D-10 | Search scope | Library only in R1. Tenant content is not indexed | Keeps tenant text out of embeddings until D-07 is settled | At R2 |
| D-11 | Evidence delivery | Stream through permission-checked endpoints, no presigned download links | One permission check and one audit row per download | Nothing |
| D-12 | Translations | Rows keyed by language, original marked, machine translations labelled | Five languages do not fit in columns | Nothing |
| D-13 | Statuses | Tenant sub-statuses inside fixed categories, no workflow engine | Flexibility without moving the guards auditors rely on | Nothing |
| D-14 | Library curation | Platform role `library_editor` in the console. Tenants report problems and propose. **Narrowed 2026-09-20 by Alex (D-62, ADR 0054):** bleqq staffs the role with nobody; an independent agent gives the second pair of eyes, and the role stays for the proposals a person takes over | The library is shared | Answered: nobody. Confirm again if bleqq ever hires for it |
| D-15 | Branching | One branch, `main`, during the build. `staging` and `main` with a second reviewer before the first real tenant | Speed now, change control when it matters | When to switch |
| D-16 | Hosting | Railway EU West, container-only, nothing Railway-specific in code | The tenant zone may have to move into a bank's environment | Nothing now |
| D-17 | Django version | **Superseded 2026-09-19 by Alex:** the newest Django release (6.1.1 at the time), and every other package at its newest release. ADR 0025 | Alex asked for all packages and libraries at their latest versions once Phase 0 was green; Django 6 is supported by every library the stack uses (django-ninja, django-cors-headers, dj-database-url, django-stubs) | Confirmed |
| D-18 | What a participant is | A tenant-zone row naming a person or a team on a register entry or a case, with composite `(tenant_id, …)` foreign keys so the database refuses another tenant's row or a non-member. Removed softly. It puts the item on My work and in notifications and grants no access. A repeated add answers 409 `already_participant`. No participant-role vocabulary, and at most `MAX_PARTICIPANTS_PER_RECORD` per record | Everything Alex asked for is attention, not access. PostgreSQL foreign-key checks bypass RLS, so only composite keys make a cross-tenant or non-member row impossible. Soft removal keeps who took part for the case file (CAS-07) and history (REG-04). The audit guard fails a 2xx write with no audit row, so a duplicate is a 409 | That participation never grants access, and that participant roles can wait |
| D-19 | Who adds and removes participants | Adding needs `register.edit` on a register entry and `cases.contribute` on a case; removing needs the same, except that a person may always remove their own row. A member whose roles cannot read the record is refused with 422 `participant_cannot_read`. A team is not checked, because each member's own read permission filters what they see. A participation outlives a lost read permission. No new permission | It reuses the people who already edit the record or save assessment input, keeps the PRD §6 matrix unchanged, and prevents adding someone to a record they cannot open | Whether Contributors should also add participants to register entries (a new permission and a PRD §6 change), and that a participation outlives a lost read permission |
| D-20 | CAS-03 contributors | The assessment's contributor teams are the case's team participants. Each change is one add or one remove call, never a replacement of the whole list. `impact_assessment` gets no contributors column | One list of who is involved instead of two. Single calls mean a stale assessment form cannot remove a team someone added in between, which would be the silent merge CAS-08 forbids | That merging them is acceptable; the fallback is two separate lists |
| D-21 | Department and department head | A department is the designed `org_unit` of kind business area, business unit or function. Its head is `org_unit.head_user_id`, and teams belong to it through `team.org_unit_id`. The department view covers the teams of that unit and of the units below it, and their active members. Any member may open any department's view, because it is a filter, not a grant. `team_member.is_lead` is not built | The designed schema already has departments with heads above teams. Using that model avoids a second 'head' concept, and nothing would read a team lead flag once heads sit on the unit and team notices go to members | That a department is an org unit with a head (TEN-02 grows to name departments), and that team leads are not needed |
| D-22 | Notes on my work | Notes are shared COL-01 comments. On My work they sit in a panel named 'Comments and mentions', never 'Notes', and its composer says that everyone in the organisation can read them. `GET /me/comments` returns only comments on records the reader can read. Comment text never reaches logs, Sentry, audit after values, outbox payloads, webhooks, notification titles or emails. Private notes are not built | Every role holds `audit.read`, `record()` stores before and after values, and the outbox feeds webhooks and the SIEM stream. The word 'Notes' on a personal page suggests a privacy the system does not give | **Confirmed by Alex 2026-09-19** (D-60): no private notes. ADR 0028 moves from accepted by default to accepted |
| D-23 | How My work is computed and exposed | One ORM service under RLS, `backend/apps/home/my_work.py`, with no database view. Reasons are owner and participant; buckets are overdue, due soon, changes on your items, and everything else. Rows and every count come from the permission-filtered set. `GET /me/work` uses the shared pagination with an optional bucket filter. Decisions stay on Today's Decide now, fed by the queue counts on `GET /me`. The page, the reminders, the digest and the TEN-05 preview all read the same service | 'My open items' gets one definition. Counts taken before filtering would tell a role without `cases.read` how many overdue cases colleagues have | That decisions stay on Today and are not repeated on My work |
| D-24 | My work and the footprint | My work does not apply the footprint and marks nothing as outside it | FP-03 names the feed, inventory, roadmap, briefing and reports. Losing sight of your own responsibility after a regulatory scope change is worse than seeing it | That My work is exempt from FP-03 hiding |
| D-25 | What counts as 'changes on your items' | Open cases whose change has a WAT-04 link, confirmed by a person, to an obligation you are involved in, carrying that obligation as the reason; plus obligation versions applied within `MY_WORK_AWARE_DAYS` (14). From chunk 10, comments and mentions on your items join it. Unconfirmed agent suggestions and your own acts are left out | Agents propose and people decide. A fixed window needs no write on every page load and does not clash with `last_visit_at`, which Library updates uses | The 14-day window |
| D-26 | When My work and participants land, and their priority | With chunks 8, 9 and 10 (R2); nothing is pulled forward. HOM-05 and COL-04 are Must, and TEN-03 rises from Should to Must because the department view is built from teams. `Build_Plan.md` lists HOM-05, COL-04, TEN-02 and TEN-03 in chunk 8 above the cuttable items, and COL-01 stays first in chunk 10 | Nothing a person can own exists before chunk 8. Priority alone protects nothing under the descope rule, which cuts from the bottom of a chunk's list; the order of the list does | Keep R1 as planned, or reorder the build plan to bring chunk 8 forward |
| D-27 | What a market is and where it is set | Operating markets are the regulatory scope's jurisdiction terms, changed through FP-02 (preview, second person, step-up). Watched markets are a separate tenant list set in a 'Markets we watch' panel on the same screen. A market's level is computed: operating if its term is in the scope, otherwise watching if a watch row exists, otherwise not followed. There is no separate Markets screen and no market-level kind | Each fact is stored once, and four eyes stays on everything that hides records. A second screen would need an extracted request component, a flag and a second pending-request read | That 'extra attention' means the default view, and that markets belong on the Regulatory scope screen |
| D-28 | How EU rules reach a country | `Jurisdiction.parent` means 'the jurisdiction whose rules reach this one'. The seed and the fixture set Norway's parent to the EU (EEA Agreement), as SE, DK and FI already are. A record's derived terms are the term of its jurisdiction plus the terms of every jurisdiction whose parent that is. No new column and no draft logic | A bank operating only in Norway would otherwise hide all EU law. The column already exists, and deriving on the record side keeps footprint validation unchanged for every dimension | That Norway also shows EU acts not yet incorporated into the EEA Agreement (applicability stays a human decision, REG-01) |
| D-29 | Where a record's jurisdiction comes from | Derived at match time inside chunk 3's single scope rule (`library/reading.py`). Obligations and instruments take it from `Instrument.jurisdiction`; changes take it from their authority. It is never stored on a record, term proposals and tagging in that dimension answer 422, and the SQL function is unchanged: the caller appends the derived ids to the array it already passes. A record with no jurisdiction is not restricted by it | The foreign key the library already has stays the only source. Showing too much is the safe side, and a second SQL argument would mean migrating built code for nothing | Nothing |
| D-30 | How watching is protected and who sees it | Watching is a direct write under `footprint.request`, audited through `record()`, with no second person and no step-up. A repeated watch answers 409 `already_watching`. Every member of the tenant can read the list. It hides and reveals nothing by default, sets no urgency, opens no triage and notifies nobody. The jurisdiction key travels only in request bodies and responses, never in a URL path or query string | Four eyes guards changes that hide records, and watching hides nothing. The access log prints the full request line and Sentry transactions keep `request.url`, so a key in the path would leak a possible expansion plan | That watching needs no second person, and that every member may read the list even though it may reveal expansion plans |
| D-31 | How a market's level behaves when operating changes | Scope approval never writes watch rows. A market watched before it became operating reads as watched again once operating stops; a market never watched reads as not followed | A computed level with precedence needs no hook in the approval transaction and no extra error. Attention is kept wherever someone asked for it | That a market you never watched simply drops to not followed |
| D-32 | Markets and research | Platform (library) runs never read tenant markets, so broad sweeps are unchanged. From chunk 11 a tenant agent's default scope is the operating markets first, then the watched ones, with a copy stored on the run at run start; an explicit AGT-04 scope overrides it. Jurisdiction keys may reach the tenant-scoped runner; tenant text never does | The two-zone rule: a tenant's expansion plans must not steer or leak through the shared library | Confirm together with D-07 and D-08 before chunk 11 |
| D-33 | Which markets can be chosen | Only active jurisdictions the platform covers — EU, SE, DK, NO and FI today — and only countries can be watched. Any other market is a change to the platform's scope, not a tenant setting | PRD §1 and I18N-01 limit scope to the Nordics plus the EU. A new market needs authorities, sources and a content language first | **Open question:** which aspiring markets outside EU, SE, DK, NO and FI are wanted, and in which release |
| D-34 | Who receives a notification | One recipient check serves every notification kind, mentions included: active members whose roles can read the record's kind at send time, one notification per person per event whatever the number of reasons. A team named on a record reaches its active members. TEN-05 removal also ends the member's team memberships | Without it a removed member would keep receiving library titles and links about the bank's work through team rows, and so would a person who lost read permission. One check replaces one per kind | That team notices go to every active member, who can mute kinds in notification preferences, rather than to team leads |
| D-35 | What the shared library holds for a standard | Public facts only — publisher, reference, edition, dates, lifecycle, national adoptions as a note and a catalogue link — plus exactly one active conformance obligation per edition in our own words, whose `ref_label` equals the official reference and which carries exactly one standard term. No provisions, no clause or control titles, no paraphrase. One check function refuses anything else at proposal creation, at a reviewer's correction and at apply (422 `licensed_text`, `one_conformance_obligation`, `standard_term_required`); field sources on a standard's records must be URLs; a trigger on `provision` refuses any row under a standard-level instrument | The library holds sourced public facts (CLAUDE.md §1), and clause content exists only in the paid standard. A payload is stored at creation, so a check only at apply would leave pasted text in the platform database, and `library_write()` also admits seeds, the watch pipeline and INV-07, which the trigger covers | Before any standard is seeded. A lawyer confirms the official title and the conformance wording; holding clause titles would change an invariant and is a stop-and-ask, not a default |
| D-36 | How a tenant opts in to a standard | A dimension `standard` ('Standards followed') with a new tier-1 kind `opt_in`. A record carrying one of its terms matches only when the regulatory scope names that term, including when the scope has no term in the dimension. The rule lives in the matcher, so a cleared flag changes nothing, and `opt_in` is a required keyword of `in_footprint`. One term per standard, not per edition. An opt-in term may sit only on a standard's own obligation and on a change whose authority's jurisdiction kind is international (422 `standard_term_only_on_standards`). Opt-in dimensions gate at tenant level only | FP-01's rule that an empty dimension means everything would mark every standard as matching every tenant. A standard term on a law would hide binding law from every tenant that does not follow the standard, with no preview and no second person | With the PRD 0.3 wording of FP-01 |
| D-37 | How an instrument says it is a standard | An optional tier-1 instrument level kind with the single value `standard`, plus one level row with `binding_default` false. The five existing levels keep a null kind, and `binding` keeps deciding their pills. When the binding level's kind is `standard`, both binding slots read 'Standard' in the `information` tone. No relabel guard is needed, because relabel payloads carry no kind | `binding=false` renders 'Guidance, comply or explain', which is wrong for a standard. Only 'standard' has a reader; law and guidance values would be a second source beside `binding_default` that could disagree with it | The pill wording and tone, on a design card |
| D-38 | Jurisdiction for international standards bodies | One seeded row, International, with a new jurisdiction kind `international`. The jurisdiction list is seeded, not proposed. A standard's obligations carry no jurisdiction term, the markets derivation derives nothing for this kind, and it gets no mirrored taxonomy term | `Instrument.jurisdiction` and `Authority.jurisdiction` are required, and the kinds today are supranational and country. Two rules must tell an international issuer from the EU by kind, never by key: the watch rule for standard terms, and the markets derivation, which would otherwise hide every standard from any tenant with an operating market | Nothing beyond D-28's derivation, which this row constrains |
| D-39 | How the regime enforces the sector scope | `Instrument.regime` becomes NOT NULL. The seed-integrity test and the instrument-proposal apply check that it is a term of the regime dimension (422 `not_a_regime`). Chunk 3 folds the instrument's regime into obligation matching. Every change carries at least one regime term (422 `regime_required`). A standard takes the regime of the family of law it serves | `schema.sql` has the regime NOT NULL while the model made it nullable, and an instrument without a regime matches every tenant. The regime list is the one closed list that changes only through a reviewed proposal | No product change |
| D-40 | Scope edges and vocabulary inside the scope | Keep tax (tax rules for financial products and the duties a firm carries for its clients) and the AI Act (under AI and ICT), and name both in the PRD's scope sentence. The PRD's six sectors become terms of the existing, termless `licensed_activity` dimension, added by proposal. Regime terms are added only for a body of law an instrument needs, and no new legal entity types until `licensed_activity` has been checked | `licensed_activity` ('The licence under which the firm acts') exists for exactly these sectors, and the Banking and Payments regimes are added by the regulatory scope work | **Open question, answered once for both analyses:** the PRD sentence, the `licensed_activity` terms, and whether SFDR, consumer credit and the Accessibility Act as applied to banking are in scope |
| D-41 | Where the Statement of Applicability's clause and control list lives | A tenant table, `soa_unit`, under forced RLS, one row per unit per legal entity, holding the tenant's own reference and words plus applicability, reason, decision time, status, note and version, unique per scope row and reference. A unit is allowed only under a scope row whose obligation sits under a standard-level instrument (422 `units_only_under_standards`) and whose applicability is 'applies' (422 `scope_not_applicable`). Reference and title are fixed once the unit has history (409 `unit_has_history`). `gap` gains a nullable unit, and a unit's applicability is set on the unit itself (amended by D-75, which removed `applicability_request`, the request table this row extended). `tenant_obligation` keeps its own uniqueness | The tenant holds the licence and already keeps an SoA; its own references and words are its own work, and the library's `obligation_requirement` cannot hold them because its text columns sit in the shared library. Units as extra `tenant_obligation` rows would inflate HOM-01's standing, REP-01's heatmap and the review roadmap branch. Fixing reference and title stops an approved decision being moved to another control by a rename | A lawyer's view before the first bank uses units. Fallback if the answer is no: ship chunk 8 without `soa_unit` and use the conformance row per entity with the tenant's own SoA as evidence |
| D-42 | How a legal entity follows a standard | The per-entity applicability of the conformance obligation is the only record that an entity follows a standard: a confirmed 'applies' with a reason such as 'Certified'. The conformance obligation carries no entity term, so it spans every legal entity, and an entity's span leaves opt-in dimensions out. The scope row gains a reason and a decision time. A scope row is created, through `record()`, in the transaction of the write that needs it; reads never write (amended by D-75: the answer is set by one person after a confirmation dialog, and the request table this row extended is gone). Removing a standard from the regulatory scope hides its rows and deletes none | A second 'follows' record, such as a licence row carrying the standard, could disagree with applicability and would move a span-deciding fact out of the one audited applicability answer (behind four eyes until D-75). PRD 0.2 already says standards are worked through applicability per legal entity | Nothing; it settles with D-36's tenant-level gating |
| D-43 | Where a certificate lives | The entity's planned licence row, using its existing columns and adding a validity end date, a next audit date and an owner. It carries no term and decides no span. Two roadmap branches show the expiry and the next audit as 'Our deadline' with the row's owner, are left out once the row is withdrawn, and never enter the calendar feed. Surveillance audits are not a recurring duty, and REG-07 is unchanged | A certificate is held by one entity, like a licence. Evidence has no per-entity anchor, and the review branch hides compliant rows, so a certified, compliant entity's next audit would vanish from the roadmap there. The calendar feed carries public facts only | The owner shape (a person or a team), together with D-21 and TEN-03 |
| D-44 | How many applicability requests are decided at once | **Superseded by D-75**, with ADR 0038: nothing is requested, so nothing is decided in bulk. A paste of many rows is one call the person confirms, with one audit event per row, and `REGISTER_BULK_MAX` still caps it. What it said: No batch table. One route, under `applicability.approve` and a step-up, takes a list of request ids with a decision and a note, and decides each through the single-request logic in one transaction. A request the caller filed answers 409 `four_eyes_violation` and nothing is decided. One `record()` per row, citing the assertion. `REGISTER_BULK_MAX` caps it at the page maximum. A pending request cannot be edited; the requester withdraws it | Step-up is a freshness window, not a token spent per action, so hundreds of prompts were never the problem. A batch table would duplicate each request's status, decider and four-eyes check, and a cap keeps a call inside the 250 ms budget | The cap |
| D-45 | How standards are watched | One change per edition or amendment, keyed by stable key, with the draft, final draft, publication and accreditation transition rule as timeline entries and the end of the transition as the key date. Standards-body sources get their own source kind and are registered inactive, with no automated check, until the lawyer answers; a person enters edition facts as instrument proposals. A source document on a host in `STANDARDS_PUBLISHER_HOSTS` keeps no snapshot, and a change carrying an opt-in term may link only documents without one (422 `licensed_text`). The prompt forbids fetching, quoting, summarising, translating or restating a standard's text, and treats a block or challenge as a failed source check | Publishers reportedly reserve text-and-data mining and blocked automated fetches during the research. Source checks hold no snapshot, but source documents do and have no source link, so a flag on the source kind could not stop them | A lawyer's view on automated monitoring of publishers' pages, and which publishers may be read automatically. Until one is cleared, a revision opens no case, because change registration is agent-only |
| D-46 | When the Statement of Applicability becomes a document | In R2 the register filtered by standard and entity is the SoA, showing per unit the reference, title, applicability, reason, status, approver and decision date; each unit's decided requests are its history, and there is no 'as of'. The dated export is R3's filtered inventory export (REP-02), with no new export kind. An existing SoA enters through the chunk 8 paste, and REP-03 is unchanged | Filters need no new code path, and units keep one entry path | **Open question:** whether to pull a CSV export into chunk 8, because banks need the document at their certification audits |
| D-47 | Which standards are seeded | ISO/IEC 27001:2022 only, in the demo fixture, once its facts are fetched and logged in the Verification log, and with no relation rows. A 'replaces' relation type is added by a vocabulary proposal when a second edition is entered; 'amends' is not added, because an amendment that keeps the edition is an obligation version. Other standards come by proposal, and payment-card and messaging-scheme standards stay out until their licence terms are read | Prove the pattern on one standard first. A lineage row needs a second instrument to point at | **Open question:** the seed list, and whether assurance reports a tenant issues are in scope at all |
| D-48 | Whether urgency levels can be added or re-ranked | The five urgency levels are fixed rows with the tone each already has. A library editor may change only a level's labels, usage note and SLA days, through a proposal. Creating a level, and a relabel carrying an ordinal, answer 422 at `/vocab/urgency`, at `POST /proposals` for a person or an API key, and inside `proposals/apply.py`. `GET /vocab` entries gain `fixedRows` so the screen hides Create and reorder, and one tone-by-kind function serves the vocabulary, roadmap and change presentations | It keeps 'tones are chosen by slot or kind, never by a person' without an exception. No requirement asks for another level, and today's code allows one only by accident | Answered OK by Alex 2026-09-19 (item 1). No ADR: it confirms the fixed rows INPUT_DELTAS §1 already names |
| D-49 | How platform staff enter a bank's tenant | A platform admin requests read-only access to one bank with a purpose and a limit of up to `SUPPORT_ACCESS_MAX_HOURS` (4); a tenant admin approves it with a passkey under `security.manage`, or declines, and may revoke it at any time. A CHECK keeps the approver from being the platform person, a guard trigger refuses deletes and edits after insert, and the support principal holds read permissions only, with no search, Ask, export or evidence download. Every request under the grant writes a `support_access.read` audit row in the bank | ADM-S6 and the designed schema both make the tenant admin the approver, and it keeps 'platform staff have no bypass' true. A grant with a stated purpose and a log the bank reads fits a processor acting on the controller's documented instructions | Answered OK by Alex 2026-09-19 (item 2). ADR 0042, PRD 0.4 |
| D-50 | Who reads a bank's problem reports, and how a library error is found | A problem report stays inside the bank that filed it: no platform read window, and no bleqq editor, other bank, agent or model reads it. A library error is found instead by the watch agents, which re-check the library records of the sources they check on every run and propose a correction through the normal proposal door, with four eyes and never a direct edit. The console loses its problem-report surface | Alex's answer replaces the recommended read window: bank comments belong to the bank, and the watch runs already visit the sources a record came from, so the re-check costs one comparison per record instead of an exception to the tenant fence | Answered by Alex 2026-09-19 (item 3), replacing the recommendation. ADR 0043, PRD 0.4. **Open:** whether bleqq's editors may not read reports either, read so from 'in the bank only' |
| D-51 | How the search index is protected | `SearchChunk` is derived data, not a library model: a plain model with `owner_tenant_id` copied from the indexed record (always NULL in R1), forced RLS in the agents 0001 shape, and a runtime fence `index_write()` that an AST guard allows only in `search/indexing.py`. Library rows are read in `search/sources.py`, which writes nothing, so the library fence is unchanged and keeps watching both modules | A chunk is neither a sourced fact nor proposed, so as a library model it would put a second writer behind the proposal door; allowlisting the indexer would let it write any library table | Answered OK by Alex 2026-09-19 (item 4). ADR 0044, INPUT_DELTAS row |
| D-52 | The calendar feed's address and contents | `GET /api/v1/calendar/feed.ics?token=<prefix>.<secret>`, 256 bits, stored as a prefix plus SHA-256 and shown once; the path form is not built. A member with `roadmap.read` keeps up to `CALENDAR_FEEDS_PER_USER` (5) feeds and creates one only after a recent sign-in or a step-up. The feed carries public regulatory dates only, never an internal deadline, owner, urgency or So-what text. A feed is revoked when the member leaves, is re-enrolled or loses `roadmap.read`, and expires after `CALENDAR_FEED_IDLE_DAYS` (30); there is no scheduled rotation | Calendar apps subscribe only by web address, so the secret has to ride in the URL. The query string stays out of our own logs and out of the hosting edge's request line, which a path token would not | Answered OK by Alex 2026-09-19 (item 5). ADR 0045, the CONVENTIONS 3.6 exception with the feed package |
| D-53 | How long a record is kept | A record is deleted ten years after its last use: closed and dismissed cases with their children, removed evidence, and the tenant's audit, published outbox and login rows. Footprint history is kept for the tenant's life and library tables are never touched. One `SECURITY DEFINER` function owned by `cw_migrator`, with `search_path` pinned and EXECUTE granted to `cw_app` only, deletes ledger rows whole and refuses any cutoff younger than one year; a daily `@tenant_task` runs it in bounded batches and writes one run row and one audit row with counts only | Alex's answer replaces the per-bank period: one fixed age, measured from a record's last use, is what every bank gets. Keeping personal data no longer than necessary needs the ledger rows to go too, since `record()` keeps before and after values | Answered '10 yrs after last use' by Alex 2026-09-19 (item 6). ADR 0046, PRD 0.4 AUD-04, CLAUDE.md §5 exception. **Open:** what counts as 'use' (proposed: the latest of closure, removal, a change, or a reference from a live record; reads do not count) and whether a bank may change the ten years (proposed: no) |
| D-54 | Which processing locations the production boot refuses | On every deployed environment except `ENVIRONMENT=test`, the boot refuses any processor outside an EU member state, from allowlists fixed in code: the `anthropic` provider and `managed_agents` are refused, `bedrock` is allowed only in an EU region with an in-region model id, Sentry only on `ingest.de.sentry.io`, mail and storage hosts only from reviewed lists, and Railway only in `europe-west4-drams3a`. Production agents therefore run in our own worker on Bedrock in an EU region | Anthropic's own API offers US or global inference only, and a transfer must never follow from a changed environment variable. A list in code makes adding a processor a reviewed change with a Verification log row | Answered OK by Alex 2026-09-19 (item 7). ADR 0047, which amends ADR 0016 and stages ADR 0007's EU runner |
| D-55 | When a stricter passkey policy takes effect | A bank's stricter policy applies at registration at once, and at sign-in and step-up from a notice date the admin sets, 14 days ahead by default; loosening applies at once. The policy lives on `security_policy` as `credential_policy`, `allowed_authenticators` and `device_bound_from`. Our own code, not the library's result alone, enforces attestation: `packed` or `tpm` with a chain verified against the claimed model's roots, a matching AAGUID, backup eligibility unset, and `apple`, `android-key` and `android-safetynet` refused. An edit that would refuse the acting admin's own passkeys answers 409. For every bank now, the backup-eligibility flag in each assertion is compared with the stored one | No passkey registered under attestation 'none' can prove its model, so enforcing on the day the policy is saved would lock a bank out; a check at registration only would leave every existing synced passkey working forever | Answered OK by Alex 2026-09-19 (item 8). ADR 0048, which carries ADR 0003's third tranche |
| D-56 | How a bank's tenant is deleted when it leaves | Two different people holding `security.manage` request and approve the exit, each with a passkey, and every admin is notified. The tenant becomes `closing`: mutating routes answer 409 `tenant_closing` while sign-in, step-up, session and key revocation, exports and downloads keep working, and the final full export is taken. After `TENANT_EXIT_DELAY_DAYS` (30) the platform operator runs `execute_tenant_exit` as `cw_migrator`, deleting every tenant row including the audit trail, the outbox, the security log and footprint history, and leaving a tombstone report with counts, the export hash and the people involved | One stolen passkey must not be able to erase a bank. Returning the data and then deleting all of it, ledgers included, is what a processor owes a controller at the end of a contract, and running it as the schema owner keeps `cw_app` unable to wipe ledgers | Answered OK by Alex 2026-09-19 (item 9), including deleting the audit rows. ADR 0049, PRD 0.4 REP-04, CLAUDE.md §5 exception |
| D-57 | Who approves a bank's private records | A compliance officer proposes and a second person in the same bank approves with a passkey under a new tenant permission, `private_records.approve`, which is never a platform grant and never an API key scope. The server sets `proposal.owner_tenant_id` from the target, so shared proposals filed from a bank still reach the console. No owned row or child of one is indexed, embedded, reranked or sent to a model, and find-similar returns shared records only. A private source is a public page over https, checked at the request and again at the start of each run, capped by `PRIVATE_SOURCES_MAX` | A platform reviewer would need a bypass into tenant rows and would read bank-confidential material. The same proposal table, apply code, four-eyes constraint and step-up keep the proposal door and four eyes intact | Answered YES by Alex 2026-09-19 (item 10). ADR 0050, PRD 0.4 INV-07, PRO-03, WAT-06 and the new permission |
| D-58 | How SSO sits beside a passkey-only product | SSO proves who a person is; the passkey stays the only way in. For addresses on a bank's verified domains the identity provider replaces the emailed code at enrolment and re-enrolment, and with enforcement on it must also pass at every sign-in, after the passkey. There is no sign-in that starts at the provider, unsolicited responses are refused, and SSO never recovers a passkey or replaces step-up. A link is made only by SCIM provisioning or at an invitation or re-enrolment, never by matching an email claim. A SCIM key is an API key with the single scope `scim`, created only on the SSO route, and its default role may hold no admin permission | It keeps 'a passkey is the only way in' word for word while the bank's directory controls who may enrol. Issuer and subject are the only stable identifier, and an email claim must never decide access | Answered YES by Alex 2026-09-19 (item 11). ADR 0051, PRD 0.4 ID-02 and ID-12 |
| D-59 | What the console sees of each bank's figures | Usage, failed jobs and stream lag per bank, read through one SELECT-only window on two tables that hold numbers, kinds and times only. `tenancy.platform_counters(principal)` opens for a platform principal holding `tenants.manage` or `system.health`, refuses while a tenant is active and clears afterwards; an AST guard limits the callers to two modules. A structural test refuses a free-text or untyped JSON column on either table. Each console read writes a platform audit row recording the screen and filters, never the figures | The console is asked for the same usage figures, failed jobs with a retry and stream lag per bank. Tables that cannot hold text turn 'tenant content never reaches the platform' into a structural guarantee | Answered OK by Alex 2026-09-19 (item 12). ADR 0052 |
| D-60 | Private notes on My work | There are no private notes. Notes on My work are the shared COL-01 comments in the 'Comments and mentions' panel, whose composer says that everyone in the organisation can read them | A private note would need a per-user policy, audit and outbox rows without the text and an exclusion from the tenant export, and would hide compliance work from the bank's own reviewers | Answered YES by Alex 2026-09-19 (item 13). No new ADR: it closes D-22's open question and moves ADR 0028 to accepted |
| D-61 | Whose agents a bank can change | bleqq's agents, the general financial-regulation watch that feeds the shared library and re-checks it (D-50), are platform-owned and platform-run, part of the base package: no tenant switches one off, pauses it, changes its cadence, scope or budget, or edits its definition. A bank adds agents of its own for what it needs, and AGT-04's controls apply to those alone; a tenant's agent writes only in that bank's zone and never the shared library | Alex's own answer. The general watch is the product, so a bank that could pause it would silently stop its own regulatory coverage, and an agent a bank steers must not be able to write the facts every other bank reads | Answered by Alex 2026-09-19 (item 14, his own). ADR 0053, PRD 0.4 AGT-03, AGT-04 and AGT-05. **Open:** whether a tenant's AI off switch touches only its own agents (proposed: yes) and who pays for a tenant's agents (proposed: the tenant's budget cap) |
| D-62 | Who gives the second pair of eyes on a library proposal, now that bleqq staffs no editor (Alex, 2026-09-20, `OWNER_RECOMMENDATIONS.md` item 19) | The proposal stays the only door and four eyes stays a check constraint; the second principal may be an independent agent instead of a person. A platform API key bound to an agent definition holds the new scope `proposals:review`, which reaches the queue read and the approve, correct and reject routes and no library row. `proposal` gains `reviewed_by_api_key`, `proposed_by_agent` and `reviewed_by_agent` (the agent columns copied from the key by the one write path), and the check constraint refuses a row whose user, key **or** agent is the same on both sides, and refuses a reviewing key that names no agent, so two keys of one agent cannot confirm each other and two unbound keys cannot pass on nulls. A session approving still needs a fresh passkey assertion; a key cannot step up, so for an agent approval the constraint and the scope are the whole gate. The applied record carries machine-confirmed provenance naming both agents and never a person's verification; the console queue is built as designed, as the surface that watches the agents and takes a proposal over. Defaults inside the decision: a bank sees both agent names on the record, and a proposal no agent confirms simply stays open — no expiry, no auto-approval | Alex's words: "Agents do the work, and there should be several agents reading the library. A tenant/bank is then responsible for their interpretation… The agent can work from the queue as well, so who does it doesn't change the function of a queue, it's still needed." One queue with two kinds of approver keeps every invariant: the door, four eyes, the audit trail and the labelling rule. Putting the agent columns on the proposal keeps the database, not Python, the word on four eyes, which is what `proposal_four_eyes` already is | Whether a bank may see which agent confirmed a fact, what should happen to a proposal no agent confirms within a window, how many independent agents read the library, and whether a person must still confirm a first-of-its-kind change (ADR 0054) |
| D-63 | Which gates a change has to run, now that a docs-only commit still ran Playwright (Alex, 2026-09-20: "if only docs folder is changed, don't run certain checks") | CI, CodeQL and `scripts/prepush.sh` tier a change the same way, on one rule: a file may skip a gate only when it cannot change what that gate measures. Two tiers are new. Spec text (`backend/apps/*/app.md`, `PRD.md`, `docs/inputs/openapi.yaml`) is its own `specs` tier that runs the checkers parsing it and nothing heavier, so rewording a scenario no longer runs the migration graph, the suite under coverage, the search evaluation or a browser; `docs/inputs/INPUT_DELTAS.md` stays in `backend` because a live scenario test reads it. CodeQL analyses a language only when a file of that language changed, and both languages on the schedule, on a manual run and on any `.github/**` change, which is every run `scripts/ship.sh` starts. `scripts/check_ci_tiers.py` fails when the three lists drift apart | Alex asked for exemptions by what changed, and a gate that cannot see the change proves nothing by running: it only makes a spec correction cost twenty minutes and teaches people to batch spec edits into code commits. Writing the same tiers in three files is what actually rots, so a guard compares them and runs in the tier the workflow files themselves belong to | That spec text may skip the suite, and whether `docs/inputs/INPUT_DELTAS.md` should also move to `specs` once the scenario that reads it stops asserting on its text |
| D-64 | What the proposal door guards: the obligations inventory, not every table in the library zone | `ProposalDoorGuard` keeps two maps instead of one. `LIBRARY_WRITING_ROUTES` is the inventory's door and does not move: approveProposal reaches `apply.apply`, the re-verification stamp reaches `apply.apply_reverification`, and each still needs `proposals.review`, a passkey step-up and a person's session. `WATCH_WRITING_ROUTES` names the six routes that register and curate a change — createChange, addChangeDocument, updateChange, addChangeEvent, updateChangeEvent, replaceChangeObligations — and `watch_write()` is the only writer any of them may reach. Those six are exempt from the `proposals.review`-and-step-up assertion and are asserted instead to demand `changes:write` of an agent's key, through `require_change_writer` (which a library editor's `proposals.review` session passes instead) or the same scope on the key-only document route, and each one's view has to live in the watch app, so an inventory route cannot be moved into the exemption. A watch route reaching `apply`, `apply_reverification` or any other writer fails, a route in neither map that reaches any writer fails, and a watch route that loses its gate fails. The scope claim is proven against the real definitions — the constants `require_change_writer` names, read out of the index, against the table of every library model behind the proposal door — not asserted in a comment | The invariant says a proposal approved by a second, independent principal is the only door into the **obligations inventory** — instruments, obligations, provisions and their versions — with the re-verification stamp the single exception. A change is a sighting, not an inventory record: WAT-02 has an agent's key register one and curate its type, timeline and obligation links, no person proposes it, and `watch_write()` is the fence built for exactly that, refusing every library table outside the seven watch tables at runtime. The guard read "library zone" where the invariant means "the inventory", so the first watch writer failed it and the only ways past would have been real weakenings: giving an agent's key `proposals.review` and a step-up a key cannot perform, or switching the assertion off. Teaching the guard the difference adds assertions rather than removing them, and a reviewer can see what did not change — `apply` is still named only by `approve`, still needs `proposals.review`, a step-up and a person, and is still unreachable from every watch route | That a sighting an agent registers is not an inventory record, so curating a change needs the `changes:write` scope and the library editor's permission but not four eyes and not a step-up (ruling H, AC-PRO1). A reviewer who reads the exemption as a loosening should argue with this row. The two gates that make it safe are asserted, not assumed: no key scope names an inventory table, and no tenant role holds `proposals.review` |
| D-65 | Which module may leave a bank's zone to write the shared search index, now that H15 lets a session write only the zone it is in | `apps/search/indexing.py` joins the H15 zone-escape allowlist in `backend/apps/shared/tests_tenancy.py`, beside the tenancy module itself, `record()` (`apps/shared/audit.py`) and `log_event()` (`apps/identity/security_log.py`). The entry permits one thing: this module may call `tenancy.platform_zone()` to write and delete `search_chunk` rows, which belong to no tenant, while a bank is activated. That is what a bank's own reviewer approving a library proposal needs — the approval rebuilds that obligation's chunks inside its own transaction, and since the write rules were split a session inside a bank's zone would read the shared chunks through the `FOR SELECT` policy and then write none of them. What still refuses: `index_write()` is the only context in which a chunk may be written at all and the AST guard in `apps/search/tests_index_fence.py` allows it in this one module, so no other code can use the opening; `apps/search/sources.py` drops a record a bank owns, and one whose instrument a bank owns, before a chunk is ever built, so nothing tenant-owned can ride out of the zone this way; the module writes no library table and the library fence is untouched by it; and the allowlist is a fixed set of four file paths, so a fifth module that wants the same opening fails the guard test rather than acquiring it silently | The rebuild has to be inside the approval's transaction or the index is stale exactly when a reader trusts it most, and the chunks it writes are shared because the record they copy is shared. The alternatives were worse: dropping the rebuild out of the transaction gives a window in which search answers text the library has already replaced, and giving `search_chunk` a tenant-owned row per bank would copy shared library text into fifty zones. This is the same shape the audit trail and the security log already have — one door into a ledger, and the zone stepped out of inside that door and put straight back | Recorded 2026-09-21 from the `c7-search-index-apply` review, which found the allowlist widened with no decision behind it. The opening itself was reviewed as part of owner item 4; no new ADR, because D-51 and ADR 0044 already settle what the index is and how it is fenced, and this row records only which zone it writes in |
| D-66 | Where the "So what?" comes from | The agent that reads a change writes the So what with it, through the door it already writes through, and reports the model, the model version and the citations it used, which the write records as the `AiGeneration` row AUD-02 asks for. Our own backend no longer calls a model a second time over the same facts on `change.registered`. Everything else stands: one draft per change from library facts only, no footprint term, tenant name, entity, product or tenant-written text in the prompt, labelled machine output until a person confirms it, and copied unconfirmed into each bank's case | An agent that sighted a change has just read the source, so it is the cheapest and best-informed place to say what the change means; a second call from a handler of ours is a second moving part, a second cost and a second thing to keep in step, for an answer over the same facts. It also keeps the model call inside the run that is already logged and budgeted | Alex, 2026-09-21, in chat. One consequence to hold onto: the model metadata is now self-reported by the agent rather than observed by our wrapper. In R1 every agent is bleqq's own, so that is a reporting boundary; it becomes a trust boundary the moment a bank runs its own agent against the route, which is the ACC work in R2, and the route's description must say so. (ACC is PRD 0.5's agent access, not on main: its design is parked in commit `cee7cf2` on `origin/claude/r1-integration`, waiting for Alex) |
| D-67 | When the test suite runs: per task or per release | Grouped, not per task. An agent building one task runs the static gates (ruff, mypy, compliance lint, requirements coverage, the API documentation gate, the secret scan) and the tests of the apps it touched, and stops there. The whole suite, the coverage floors and the E2E journeys run once over a merged batch, before the ship that carries it, and again in CI on `candidate`, which stays the authority | Measured on 2026-09-20: the whole suite is 1307 tests in 35 minutes, one agent's five-app run took 48 minutes, and up to fourteen worktrees share one database, so the same tests were being run over and over by agents whose changes could not affect most of them. Nothing is skipped and no threshold moves: the same gates run, once per batch instead of once per task | Alex, 2026-09-21, in chat. The cost is that a red batch implicates several branches at once rather than one; the mitigation is that each branch's own apps were green before it merged, so a batch failure is nearly always an interaction between them, which is exactly what a per-task run could never catch anyway |
| D-68 | What the console asks for when it creates a bank (Alex, 2026-09-22: "this data should probably be provided by the tenant themself. And the 'short name' seems unnecessary") | The console's create-tenant call takes only a name and the first administrator's address and title. The timezone, default language and content languages are no longer asked of platform staff: the tenant reads Europe/Stockholm until its own administrator corrects it, and no default language or content language is set until they choose one, both on the Organisation profile screen that already exists and already self-services these three fields under `security.manage`. The short name (`slug`) is no longer typed by a person: it is derived from the name (lower-cased, hyphenated) and de-duplicated with a numeric suffix on collision, keeping the column, its uniqueness constraint and every place that already reads it | Alex's own words. The three language fields were platform staff guessing at a bank's own working language before the bank had said a word, and `onboarding()`'s `profile` step already exists to catch exactly this: it reads `default_language_id is not None` and a content-language row existing, so a bank that has not set them shows an open onboarding step rather than a silent guess standing in as fact. The short name was never read from anywhere but the database (no route or URL keys off it despite its own field description claiming otherwise, which this change also corrects) — asking a person to invent a customer-number-like value they cannot revise was configuration nobody asked for | Whether a bank should also be able to rename its short name later. Today it is fixed for the life of the organisation, as it was before this change; deriving it removes the one moment a person chose it, which makes that fixedness easier to notice. Not decided here |
| D-69 | Whether a banking group's regulatory scope is shared or per legal entity (Alex, 2026-09-22: "I need to be able to easily handle several companies that are part of a group, like the SEB structure"; "there are users that will be cross several of the entities, so they need easy access") | Each legal entity narrows the group's regulatory scope for itself, rather than every entity in the tenant sharing one footprint. A legal entity's effective scope is the tenant footprint intersected with whatever it narrows further — an entity may only remove from the group's scope, never add outside it, mirroring FP-01's own dimension rule at one level down. A member's access to an entity is a separate axis from scope: a membership carries an entity scope that is either every entity in the tenant (a group compliance function) or a named subset (a local team), so the same person is not forced to choose one entity to work in and lose the rest | SEB is the worked example: SEB AB (banking), SEB Fonder (fund management) and SEB Liv (insurance) sit under one banking group, each regulated differently, and a single shared footprint would either show every entity every regime or force every narrowing decision through per-obligation applicability alone (REG-01, chunk 8) with no way to pre-scope by entity. Alex asked for both pieces in the same breath — the narrowing and the people who cross entities — because a design that solves one without the other is not what a group compliance function actually needs | **Not built. Design only, and incomplete even as design.** This reshapes code already shipped in R1: `FootprintTerm` (`apps/taxonomy/models.py`) is tenant-scoped only today, with 11 call sites across cases, library, search, watch and taxonomy reading `footprint_of(tenant_id)`; every one needs an entity argument or a documented default. `LegalEntity` itself does not exist yet (TEN-02, chunk 8) — this decision has no table to attach to until that lands. `Membership`'s entity-scope column, whether narrowing a footprint takes FP-02's four eyes and step-up or a lighter gate, and how a cross-entity member switches between entities on screen are all open. This needs its own planning pass — a brief like `docs/plans/briefs/AGENT_ACCESS.md` (not on main: it is parked with the agent-access design in commit `cee7cf2` on `origin/claude/r1-integration`) — before any code, not a slice added to an unrelated push |
| D-74 | Who confirms a watch item's curation, now that bleqq staffs no editor at all (Alex, 2026-09-21: "the bleqq staff part, that's optional as I will probably have agents managing the library"; closes `q-editor-confirm`, his item 16) | An agent of a different definition and key confirms, and the item reads **machine-confirmed**, naming the suggesting and the confirming agent, exactly as an applied library record does under D-62. It is the same separation the proposal door uses, applied where there is no proposal: the confirming key holds the review scope, reaches the curation routes and no library row, and the one write path copies the agent from the key so two keys of one agent cannot confirm each other and an unbound key cannot pass on a null. What a curation confirmation still is not: it is not four eyes, because a check constraint guards a proposal row and there is no proposal here, and it is not a person's verification, so no screen may render it as one. Unblocks WAT-S4, WAT-S6, INV-S14 and PRO-S13 | Alex's own answer, and the only one consistent with the rest: item 19 left the role unstaffed, so "a library editor confirms it alone" named nobody, and a suggestion nobody can ever confirm would leave every bank re-deciding the same obligation link for itself. The machine-confirmed label is what keeps the widening honest — the confirmation gets cheaper, and the record says so, rather than borrowing the weight of a person's verification | That a curation confirmation carries no four eyes behind it. The proposal door is unchanged and still constrained; this is the one confirmation in the product that rests on the scope separation alone. If that proves too thin in the first assurance review, the way back is to make curation a proposal like everything else, which costs a queue and nothing else |
| D-75 | Who decides whether an obligation applies to the bank (Alex, 2026-09-22: "does not need a second sign-off with passkey. That is overkill for 'what applies'. It is enough that one compliance person sets it, with a confirmation dialog before it's stored, and that an audit event was recorded") | One person holding `applicability.approve` sets applicability — per obligation, per legal entity and per unit of a standard — directly, after confirming it in a dialog. The write stores the value and reason at once and records one audit event through `record()` naming the person, the value before and after and the reason. No request, no "Waiting for approval", no second approver, no passkey step-up. A paste of many rows is one confirmed call with one audit event per row, capped by `REGISTER_BULK_MAX`. `applicability.request` is retired when chunk 8 builds REG-01; the permission and the audit event are the whole gate. This supersedes D-44 and ADR 0038, and amends D-41 and D-42 and ADRs 0035 and 0036, which extended the request table | "Applies" is a scoping judgement that a compliance person owns and revisits, not a sign-off: four eyes and a step-up made the commonest register edit a two-person ceremony, and a bank maintaining hundreds of obligations would work around it. "We comply" stays a separate fact (CLAUDE.md §5), so a gap still cannot hide behind a wrong answer here, and the audit trail shows who said what and when. Four eyes stays on what it guards in the invariant: approvals, sign-off, footprint changes, exports, keys, roles and security | None. Risk acceptance (`risk.accept.approve`) keeps its four eyes; this decision is about applicability only |
| D-78 | Which code may leave a bank's zone, now that H15 lets a session write only the zone it is in (amends D-65; `identity-agent-keys-backend`) | The H15 guard (`test_only_record_leaves_the_tenant_zone` in `backend/apps/shared/tests_tenancy.py`) allows seven entries and no others, each for one reason. **`apps/shared/tenancy.py`** defines `clear_tenant()` and `platform_zone()`. **`record()`** (`apps/shared/audit.py`) and **`log_event()`** (`apps/identity/security_log.py`) are the one door into the audit trail and the security log: each steps out of the zone to write a row that belongs to no tenant and steps straight back. **`apps/search/indexing.py`** writes the shared search chunks, derived only from shared records (D-65). **`apps/watch/write.py`** is the watch door's upsert: `source` is the one watch table with a zone column (WAT-06), and the upsert must not depend on whichever tenant a shared connection has active. **`apps/shared/e2e_seed.py`** writes both zones on purpose and refuses to run on a deployed environment. **`apps/identity/api_keys_logic.py` is allowed three functions, not the module**: `resolve_api_key`, because a platform key's request asserts the no-tenant zone its row lives in rather than inheriting whatever the connection last had, and `create_agent_key` and `revoke_agent_key`, because a platform agent key's row belongs to no tenant and both are reached only by a platform session holding `agent_definitions.manage`, which no bank session can hold. The guard now reads an entry as a module or as one top-level function in it, so a new caller in that module fails until it is reviewed | Three of the seven (`watch/write.py`, `e2e_seed.py`, `api_keys_logic.py`) had joined with a code comment and no decision behind them, which is what D-65 was recorded to stop; and a module-wide entry for a module that also writes a bank's own keys allowed far more than the one reason it was added for. Naming each entry is what lets a reviewer disagree with one, and narrowing the one module that mixes both zones is what keeps a bank's key functions from ever leaving their bank | Nothing to decide now. The guard change is given a security look before the merge. `watch/write.py` and `e2e_seed.py` stay module-wide; narrow them the next time either changes |
| D-79 | Which proposals an agent may approve, while only an obligation version can say an agent confirmed it (`proposals-decide-hardening`, 2026-09-23) | An agent's key holding `proposals:review` approves and corrects `new_obligation_version` proposals only. Approving a `vocabulary_*` or `term_*` proposal answers 409 `person_review_required`, applies nothing and leaves the proposal open for a person holding `proposals.review` with a fresh passkey; an agent may still reject one, because a rejection writes no library row. The rule sits in `proposals/logic.approve` and is keyed on the kinds that can carry machine-confirmed provenance (`OBLIGATION_KINDS`), so a kind added later waits for a person until someone widens it on purpose. Under an agent's approval every translated summary is stored labelled machine-made, whatever the payload's `isMachine` says, since no person confirmed it, and an agent's correction may not move `originalLanguage` (422 `validation_error`), which would otherwise store a translation as the unlabelled original | The invariant is that a record an agent confirmed carries machine-confirmed provenance naming both agents and never reads as verified by a person (INV-05, D-62, ADR 0054). `obligation_version` has `verified_origin` and `verified_by_agent` for that; a vocabulary row, its labels and a taxonomy term had no such columns when this was decided, so an agent's approval of one would have been indistinguishable from a person's. Refusing is the only default that cannot mislabel anything, and it costs little while the library's lists are seeded and rarely proposed to. Since `vocab-term-provenance` (2026-09-23) every library list and `taxonomy_term` carry `verified_origin`, `verified_by_agent` and `applied_by_proposal` (taxonomy 0007), stamped by the approval that writes their wording, with an agent's translations stored machine-made, and the vocabulary and term reads expose them; the refusal itself is unchanged, because lifting it is the owner's call and his answer has not reached the repository in his own words | Whether vocabulary rows, their labels and taxonomy terms should gain machine-confirmed provenance columns so agents can confirm them too (a migration on every library list, the registry and the read schemas), or whether list changes stay a person's decision for good. The columns now exist; what remains is Alex's yes, in his own words, to lift the 409 (TODO_FOR_alex.md, `vocab-term-provenance`). Until then, with `library_editor` unstaffed, a vocabulary or term proposal waits in the console queue until Alex or someone he names approves it |
| D-80 | How a confirming agent's decision is logged, and where that agent lives (agents-confirmer-definition, 2026-09-23; follows D-62, D-66 and D-74; PRD AUD-02, PRO-02) | An agent's review decision — approving, correcting or rejecting a proposal, or confirming a watch item's curation — reports the model call behind it as D-66's "So what?" does: one `AgentDecision` object (`apps/shared/schemas.py`) carrying the model, the model version, the prompt's name and hash, the output and at least one public citation, refused without a model, a version or a citation. The write that takes it logs one `ai_generation` row under the new purpose `agent_review`, with `model_metadata_reported_by_agent` true, the decision's subject named, and the open run it was made in as `agent_run`: a run of the deciding key's own agent, so a queue's decisions are counted per run as a sweep's registrations are. The database holds the first three (the check constraint `ai_generation_agent_review_names_its_run`); that the run is the deciding key's own is the consumer's to check and test. No bank reads the row: a decision on a proposal is about the queue, where a bank sees only what it filed itself, and a curation confirmation follows the same one rule, so the library read policy on `ai_generation` leaves `agent_review` out and the platform alone reads it in its own zone (governance 0002; TODO_FOR_alex asks whether a bank should see the reasoning behind a record it reads). The deciding definition ships as `backend/agents/library-confirmer/v1/`, of the new agent kind `review`: its own definition, prompt and key, never `proposals:write`, the same model allowed (TODO_FOR_alex default), a draft until its evaluation rows exist. The consumers — the approve, reject and curation bodies that carry the object — land separately | AUD-02 asks that a confirming agent's approval, correction or rejection be logged with the model behind it, and a decision by a model is a model call like any other. Without a purpose of its own the log could not tell a machine's approval from a machine's draft. One shape for both places a confirming agent decides keeps the contract one thing to document and validate. Reported rather than observed for D-66's reason: the agent made the call, not bleqq's wrapper, and the boolean is what tells a reader so | Nothing blocks. That the metadata is self-reported: a reporting boundary while every agent is bleqq's own, a trust boundary the day a bank runs its own reviewing agent (R2's agent-access work) |
| D-82 | How a streamed model call that did not finish is logged: an Ask answer the reader left, or one the model failed part way (search-ask-backend, 2026-09-23) | Once the model has been asked, the call leaves its one `ai_generation` row however it ends: the words the model had written by then, the passages they cite, the model the call was addressed to, and no token counts, because the provider reports usage only with its last word. The row says how the call ended in a column of its own, `stop_reason` (`stopReason` on the AI log): the provider's own word when the model finished (`end_turn`, or `max_tokens` when it stopped at the limit), `aborted` when the reader left first, `failed` when the model failed. `status` stays `draft`: it is the review state, how far a person has got with the output, and neither ending is a review. `ai.generate`, whose row shares the caller's transaction and whose words reach nobody before the model has finished, still logs nothing when the model fails | AUD-02 and CLAUDE.md §5: every model call is logged. A streamed answer's statements reach the reader as they are written, so a call cut short by the reader or by the model has already shown AI output the log must hold, and a row that read as a finished call with no usage would mislead a vendor review or a cost roll-up | `stop_reason` is additive on `ai_generation` (governance 0003) and on `AiGenerationRow`. What the stream itself tells a reader whose answer the model cut off at `ASK_MAX_TOKENS` is not decided: every statement sent is still cited and the answer closes as any other, and only the AI log says it was cut short |

**Numbering.** D-70 to D-73, D-76 and D-77 are reserved for PRD 0.5's agent-access
decisions, as are PRD version 0.5 and ADRs 0055 to 0057. They are parked in commit
`cee7cf2` on `origin/claude/r1-integration`, waiting for Alex (`docs/TODO_FOR_alex.md`,
"Parked for your review"). Main holds only their summary, so that branch is kept until
they have landed or Alex has declined them. They land with these numbers, matched by
title and never by arithmetic: that branch numbers agent access D-68 to D-73, and
because main took D-68 and D-69 first, its "What a bank's own agent may read" becomes
D-76 and "How a bank's own agent authenticates" becomes D-77, while D-70 to D-73 keep
their titles and numbers. D-78 to D-81 are pre-assigned to work in flight: D-78
`identity-agent-keys-backend`, D-79 `proposals-decide-hardening`, D-80
`agents-confirmer-definition` and D-81 `ask-standard-no-answer`. A later row written on
a branch is numbered D-8x with its package named in it. The session that merges it
into main gives it the lowest number that is neither used nor reserved in this note.
It checks that number against this note, and does not work it out by counting rows.
The next new ADR is 0058.

D-18 to D-34 are PRD 0.3's My work, participants and markets decisions
(`docs/plans/briefs/MY_WORK_AND_MARKETS.md`); D-35 to D-47 are its standards
decisions (`docs/plans/briefs/STANDARDS.md`). ADRs 0026 to 0041 carry them.
D-62 is PRD 0.4's no-editors decision
(`docs/plans/briefs/NO_EDITORS_CONSOLIDATION.md`), carried by ADR 0054.
Every fact about ISO, IAF, PCI SSC, Swift and the national standards bodies
that those decisions rest on is **unverified**: it is listed in
`docs/TODO_FOR_alex.md` as a fetch to do before anything is seeded.

D-48 to D-61 are the fourteen owner decisions Alex answered in chat on
2026-09-19; the researched detail behind each is
`docs/plans/briefs/OWNER_RECOMMENDATIONS.md`, and
`docs/plans/briefs/PRD_0_4_CONSOLIDATION.md` lists everything they changed.
ADRs 0042 to 0053 carry twelve of them. D-48 and D-60 have no ADR of their
own: each confirms a design already recorded (the fixed urgency rows of
INPUT_DELTAS §1, and ADR 0028). Three answers changed the recommendation
rather than accepting it, and are marked **Open** above where a detail still
needs Alex: D-50, D-53 and D-61.
