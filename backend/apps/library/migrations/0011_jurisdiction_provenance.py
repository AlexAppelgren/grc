# Generated by Django 6.1.1 on 2026-09-25 02:59

import django.db.models.deletion
from django.db import migrations, models

"""library 0011 (ADM-02, VOC-07, INV-05, D-79, D-94): a jurisdiction row gains the version and
the machine-confirmed provenance every library list row carries (taxonomy 0007), because a
jurisdiction is now relabelled, retired and restored through an approved proposal.

`version` backs If-Match on the relabel proposal and starts at 1 on every seeded row.
`verified_origin` (`agent` or `user`, blank on a seeded row), `verified_by_agent` (the
confirming agent, when it was one) and `applied_by_proposal` (the approved proposal that wrote
the row's current wording) name who confirmed it; the proposing agent is read through the
proposal. apps/proposals/apply.py sets them on a relabel and leaves them on a retire or a
restore, which write no wording. The door that lets the proposal write the row is shared 0010."""


class Migration(migrations.Migration):

    dependencies = [
        ("agents", "0003_agent_kind_review"),
        ("library", "0010_recurring_duty"),
        ("proposals", "0007_proposal_risk_flags"),
    ]

    operations = [
        migrations.AddField(
            model_name="jurisdiction",
            name="applied_by_proposal",
            field=models.ForeignKey(
                blank=True,
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="+",
                to="proposals.proposal",
            ),
        ),
        migrations.AddField(
            model_name="jurisdiction",
            name="verified_by_agent",
            field=models.ForeignKey(
                blank=True,
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="+",
                to="agents.agent",
            ),
        ),
        migrations.AddField(
            model_name="jurisdiction",
            name="verified_origin",
            field=models.CharField(
                blank=True,
                choices=[("agent", "agent"), ("user", "user")],
                default="",
                max_length=16,
            ),
        ),
        migrations.AddField(
            model_name="jurisdiction",
            name="version",
            field=models.PositiveIntegerField(default=1),
        ),
    ]
