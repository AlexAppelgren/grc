# Generated by Django 6.1.1 on 2026-09-22

import django.db.models.deletion
from django.db import migrations, models


"""library 0007 (chunk4-T26, INV-05, INV-06, PRO-02, D-62, ADR 0054): machine-confirmed
provenance beside the existing re-verification stamp.

`instrument`, `obligation` and `obligation_version` gain `verified_origin` (the existing
`origin_type` kind) and `verified_by_agent`. `proposals/apply.py` sets both, with the
version, when an approving proposal's `_obligation_version` runs: `agent` and the
confirming agent's id when an independent agent approved, `user` and null when a person
did. The proposing agent is not repeated here: it is read through
`obligation_version.applied_by_proposal.proposed_by_agent`, so both agents are named
without a third column. `instrument` and `obligation` gain the same two columns for the
shape schema v0.3 names; nothing writes them yet, because no proposal kind changes those
rows directly."""


class Migration(migrations.Migration):

    dependencies = [
        ("library", "0006_problem_report_writes_its_own_zone"),
        ("agents", "0002_library_rows_visible"),
    ]

    operations = [
        migrations.AddField(
            model_name="instrument",
            name="verified_origin",
            field=models.CharField(blank=True, choices=[("agent", "agent"), ("user", "user")], default="", max_length=16),
        ),
        migrations.AddField(
            model_name="instrument",
            name="verified_by_agent",
            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.PROTECT, related_name="+", to="agents.agent"),
        ),
        migrations.AddField(
            model_name="obligation",
            name="verified_origin",
            field=models.CharField(blank=True, choices=[("agent", "agent"), ("user", "user")], default="", max_length=16),
        ),
        migrations.AddField(
            model_name="obligation",
            name="verified_by_agent",
            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.PROTECT, related_name="+", to="agents.agent"),
        ),
        migrations.AddField(
            model_name="obligationversion",
            name="verified_origin",
            field=models.CharField(blank=True, choices=[("agent", "agent"), ("user", "user")], default="", max_length=16),
        ),
        migrations.AddField(
            model_name="obligationversion",
            name="verified_by_agent",
            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.PROTECT, related_name="+", to="agents.agent"),
        ),
    ]
