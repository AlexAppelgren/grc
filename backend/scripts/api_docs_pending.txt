# What the published API does not explain yet, against the standard in
# docs/plans/briefs/API_DOCUMENTATION.md. One line per schema class or operationId:
#   schema|operation <name> app <app> <reason>
#
# THIS FILE MUST BE EMPTY BEFORE R1 IS CALLED DONE. It is the list of places where an
# integrator at a bank still has to guess, and every one of them is a support call.
#
# A line is deleted when its schema or operation is documented to the standard; the gate
# fails on a line that is already documented, so the ledger can only shrink. Generated by
# scripts/api_docs_gate.py --write-pending, which keeps the app and reason already here.
#
# Sweeping one app: `python backend/scripts/api_docs_gate.py --app <app>` ignores that
# app's lines and reports exactly what it still owes, touching no file. When it goes
# green, delete that app's lines here and commit them with the descriptions.
#
# "app unknown" is a schema no operation references (a Ninja Query model inlined into
# parameters, shared by every app); the sweep that documents it names its app.

operation approveFootprintRequest app taxonomy 4 findings (description, operations)
operation createFootprintRequest app taxonomy 4 findings (description, operations)
operation createTerm app taxonomy 3 findings (operations)
operation getFootprint app taxonomy 3 findings (operations)
operation listJurisdictions app taxonomy 3 findings (operations)
operation listTaxonomyDimensions app taxonomy 3 findings (operations)
operation listTerms app taxonomy 5 findings (description, operations)
operation rejectFootprintRequest app taxonomy 4 findings (description, operations)
operation updateTerm app taxonomy 4 findings (description, operations)
operation withdrawFootprintRequest app taxonomy 4 findings (description, operations)
schema FootprintDecisionBody app taxonomy 1 finding (description)
schema FootprintDryRun app taxonomy 4 findings (description)
schema FootprintPreview app taxonomy 2 findings (description)
schema FootprintRequestBody app taxonomy 2 findings (description)
schema FootprintRequestRow app taxonomy 11 findings (description)
schema FootprintTermRef app taxonomy 4 findings (description)
schema FootprintTermSelector app taxonomy 2 findings (description)
schema FootprintView app taxonomy 2 findings (description)
schema JurisdictionRow app taxonomy 5 findings (description)
schema PersonRef app taxonomy 2 findings (description)
schema TaxonomyDimensionPage app taxonomy 2 findings (description)
schema TaxonomyTermCreateBody app taxonomy 5 findings (description)
schema TaxonomyTermPage app taxonomy 2 findings (description)
schema TaxonomyTermQuery app taxonomy 2 findings (description)
schema TaxonomyTermRow app taxonomy 12 findings (description)
schema TermRef app search 3 findings (description)
