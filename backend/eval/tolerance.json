{
  "_comment": "How far each metric may fall under its recorded baseline before search_eval.py fails CI. Absolute points on a 0 to 1 scale. The set is small (53 questions, 60 texts), so one question is roughly 0.02 of recall; 0.05 lets a reranker or embedder swap lose two questions without a red build and catches a third. The screen tolerance is 0: an embedded-instruction case that stops being flagged is a security regression (AGT-07), never noise. The two AGT-08 metrics carry their own reasons in _rationale.",
  "_rationale": {
    "classification_in_scope_accuracy": "0. An off-sector text read as inside the scope is a document the agent would register, which AGT-08 forbids, and an in-scope text read as outside it is a rule that never reaches a bank; neither is noise. The metric is the mean over all 60 texts, where one text is 0.017, so a tolerance of 0.017 or more lets one of the four off-sector texts be registered without a red build, and any smaller one acts as 0 at this size. At 0 the number of texts judged wrong can never grow past the recorded baseline. What 0 does not catch: a change that puts one text right and another wrong keeps the mean, so read the report's lines by kind of text (off_sector, cites_standard, standard) to see which moved.",
    "classification_standard_term_accuracy": "0. The opt-in term decides who sees a record. On a law it hides that law from every bank that has not opted in to the standard, so a bank misses a rule that binds it; missing from a standard's own record, it shows the standard to banks that never chose it. Three standards texts and one law citing a standard carry the rule, and one wrong text is 0.017 of the metric, so any tolerance that let a drop pass would let one of those four fail unseen. Like the screen, a wrong term is a visibility defect and never noise."
  },
  "metrics": {
    "retrieval_recall_at_10": 0.05,
    "retrieval_mrr": 0.05,
    "classification_change_type_accuracy": 0.05,
    "classification_flags_accuracy": 0.05,
    "classification_scope_accuracy": 0.05,
    "classification_screen_accuracy": 0.0,
    "classification_in_scope_accuracy": 0.0,
    "classification_standard_term_accuracy": 0.0
  }
}
