'use client';

// "Does it apply to us?": the answer per legal entity, its reason and who set it (REG-01, D-75).
// ObligationScreen mounts it once, at its place in design/screens/tenant-obligation.html;
// the panel's own package fills this file and never edits the page. Until then it renders nothing.
export function ObligationApplicabilityPanel({ obligationId: _obligationId }: { obligationId: string }) {
  return null;
}
