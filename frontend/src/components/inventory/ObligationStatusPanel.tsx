'use client';

// "Where we stand" and "How we handle it": the compliance status per legal entity and how the bank meets the duty (REG-02).
// ObligationScreen mounts it once, at its place in design/screens/tenant-obligation.html;
// the panel's own package fills this file and never edits the page. Until then it renders nothing.
export function ObligationStatusPanel({ obligationId: _obligationId }: { obligationId: string }) {
  return null;
}
