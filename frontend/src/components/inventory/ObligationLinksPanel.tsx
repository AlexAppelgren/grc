'use client';

// "Linked internal items": the policies, controls, processes and systems that carry the duty (REG-05).
// ObligationScreen mounts it once, at its place in design/screens/tenant-obligation.html;
// the panel's own package fills this file and never edits the page. Until then it renders nothing.
export function ObligationLinksPanel({ obligationId: _obligationId }: { obligationId: string }) {
  return null;
}
