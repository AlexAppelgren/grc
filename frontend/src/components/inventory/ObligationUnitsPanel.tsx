'use client';

// "Units": the Statement of Applicability units under a standard's conformance obligation (REG-08).
// ObligationScreen mounts it once, at its place in design/screens/tenant-obligation.html;
// the panel's own package fills this file and never edits the page. Until then it renders nothing.
export function ObligationUnitsPanel({ obligationId: _obligationId }: { obligationId: string }) {
  return null;
}
