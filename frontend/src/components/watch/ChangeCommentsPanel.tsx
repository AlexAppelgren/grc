import type { CasePanelProps } from '@/features/cases/types';

// built by c10-fe-comments-panel
// The comments on this bank's case (COL-01): a bank's change page is its case,
// so the comments hang on the case and not on the shared change. Renders
// nothing until that package fills it; the change page mounts it only when
// the change has a case.
export function ChangeCommentsPanel(_props: CasePanelProps): null {
  return null;
}
