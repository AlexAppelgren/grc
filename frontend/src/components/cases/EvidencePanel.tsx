import type { CasePanelProps } from '@/features/cases/types';

// built by c9-fe-actions-evidence
// The case's evidence and its scan state; only a checked file downloads
// (CAS-05). Renders nothing until that package fills it; the change page
// already mounts it where the design card places it (CaseWorkPanels).
export function EvidencePanel(_props: CasePanelProps): null {
  return null;
}
