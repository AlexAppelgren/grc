import type { CasePanelProps } from '@/features/cases/types';

// built by c9-fe-signoff-casefile
// Show case file and its export, in every status (CAS-07). Renders nothing
// until that package fills it; the change page already mounts it where the
// design card places it (CaseWorkPanels).
export function CaseFilePanel(_props: CasePanelProps): null {
  return null;
}
