import type { CasePanelProps } from '@/features/cases/types';

// built by c9-fe-triage-assessment
// Triage: confirm and assign, dismiss, the next step and the one-person close,
// and Move back to triage (CAS-02). Renders nothing until that package fills
// it; the change page already mounts it where the design card places it
// (CaseWorkPanels).
export function TriagePanel(_props: CasePanelProps): null {
  return null;
}
