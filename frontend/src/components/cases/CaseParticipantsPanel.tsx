import type { CasePanelProps } from '@/features/cases/types';

// built by c9-fe-case-participants
// The case's participants, people and teams, in every status (COL-04).
// Renders nothing until that package fills it; the change page already mounts
// it where the design card places it, beside the documents.
export function CaseParticipantsPanel(_props: CasePanelProps): null {
  return null;
}
