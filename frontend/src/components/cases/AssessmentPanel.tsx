import type { CasePanelProps } from '@/features/cases/types';

// built by c9-fe-triage-assessment
// The impact assessment, read-only from waiting for sign-off (CAS-03). Renders
// nothing until that package fills it; the change page already mounts it where
// the design card places it (CaseWorkPanels).
export function AssessmentPanel(_props: CasePanelProps): null {
  return null;
}
