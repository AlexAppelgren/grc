import type { CasePanelProps } from '@/features/cases/types';

// built by c9-fe-signoff-casefile
// Request sign-off, sign off with a passkey or send back, and the signed-off
// close (CAS-06). Renders nothing until that package fills it; the change page
// already mounts it where the design card places it (CaseWorkPanels).
export function SignoffPanel(_props: CasePanelProps): null {
  return null;
}
