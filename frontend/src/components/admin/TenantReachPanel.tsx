// Tenant reach (design/screens/admin-security.html, the place marked below
// Sessions): the agent access card draws this panel in its own package. Until
// then it renders nothing, so the page carries no dead control.
export function TenantReachPanel() {
  return null;
}
